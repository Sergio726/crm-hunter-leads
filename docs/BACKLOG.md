# 📋 BACKLOG — Tablero de tareas

> Fuente de verdad de qué falta hacer. **Al terminar una tarea, movela a "Hecho" con fecha.** Al agregar una, poné prioridad y agente sugerido.

**Prioridad:** 🔴 urgente · 🟠 alta · 🟡 normal · ⚪ idea/futuro
**Estado:** `pendiente` · `en curso` · `bloqueada` · `hecho`
**Agentes:** `backend-supabase` · `mobile-app` · `integrations-n8n` · `web-admin` · `orchestrator`

---

## 🔴 Urgente

| ID | Tarea | Estado | Agente | Notas |
|---|---|---|---|---|
| SEC-1 | Rotar **secreto de Google OAuth** (se mostró en texto) y actualizarlo en el proyecto cloud | ✅ hecho | backend-supabase | Confirmado en logs de auth (login OK con secreto nuevo) |
| SEC-2 | Rotar **contraseña root del VPS** Hostinger (se expuso en chat) | ✅ hecho | orchestrator | Cambiada desde panel Hostinger. SSH por clave sigue OK |
| SEC-3 | Regenerar **`JWT_SECRET`** del self-hosted antes de datos reales | pendiente | backend-supabase | Baja urgencia: solo aplica al VPS de respaldo |
| SEC-4 | Borrar archivos de secretos del escritorio (`supabase-keys-NUEVAS.txt`, etc.) | ✅ hecho | (usuario) | Completado por el usuario 2026-07-09 |
| WEB-17 | **Convertir vendedor existente en administrador** | en curso (otra rama) | web-admin | Implementado en `worktree-purrfect-pondering-bengio` (commit `3b55623`, más completo que un primer intento descartado acá: ascender + bajar rol, confirmación inline, migración `0014` con guard anti-autodegradación/anti-último-admin). Falta mergear esa rama a `main`. **Ojo**: esa misma rama reusó el ID `WEB-17` para una tarea distinta (glitch visual en `/clientes` mobile) en la sección Roadmap Web más abajo — hay que renumerar una de las dos antes de mergear |
| N8N-14 | **Bug: retry de n8n no reintenta nada** | ✅ hecho (2026-07-10) | integrations-n8n + usuario | Dos bugs en el workflow `GHL Retry`, ambos corregidos y verificados contra el servidor real (`n8n.moremigracion.com`): (1) nodo "To Push Payloads" asumía que `$input.first().json` era un array — corregido vía API, verificado con la ejecución 150 (pasó de 0 a 1 ítem producido). (2) nodo "Batch" (Split in Batches) tenía la salida "done" (índice 0) conectada a "Re-Push" en vez de la salida "loop" (índice 1) — **corregido manualmente por el usuario en el panel de n8n** (el subagente de integraciones no acepta autorizaciones relayadas por el orquestador para cambios de producción, correctamente), confirmado con `GET` posterior del workflow. Archivo local `n8n/workflows/crm-lite/ghl/retry.json` sincronizado con ambos fixes. Nota: como el pendiente de sync bajó a 0 (se resolvió el duplicado N8N-15), no hubo un caso real para ver el retry completo en acción con datos reales — la próxima vez que algo falle en el push inicial será la primera prueba end-to-end real del mecanismo ya arreglado |
| N8N-15 | **Duplicado de cliente bloqueando sync** ("Francy Diaz Ortegon") | ✅ hecho (2026-07-10) | backend-supabase | Dos filas en `clients` para el mismo lead. Resuelto: se conservó la fila `a1563759…` (ya sincronizada a GHL), se migró la interacción de la fila `f760e792…` y se borró esa fila. Verificado con `get_advisors`, sin efectos colaterales |

## 🟠 Alta

| ID | Tarea | Estado | Agente | Notas |
|---|---|---|---|---|
| GIT-1 | Commitear cambios pendientes (ARCHITECTURE.md, `.env` cloud, docs/, agents/) | ✅ hecho | orchestrator | Commits `b03a1c7` y `804d843`. Verificado 2026-07-11: `main` está al día con `origin/main`, ya no hay nada pendiente de pushear |
| N8N-1 | Renombrar `ghl_contact_id`/`ghl_synced_at` → `crm_*` (migración) | ✅ hecho | backend-supabase | Migración `0005`, trigger/índice renombrados, `types.ts` actualizado, advisors OK |
| N8N-2 | Crear **Database Webhook** Supabase → n8n al cambiar `clients` | ✅ hecho | integrations-n8n | Migración `0007` (pg_net), solo `origin='app'`. Probado end-to-end |
| N8N-3 | Workflow n8n: PUSH (upsert GHL) + PULL (buscar por tag) + TAGS | ✅ hecho | integrations-n8n | 3 workflows activos, ver `docs/INTEGRACION-GHL.md` |
| GHL-1 | Cargar secrets GHL y probar sync end-to-end | ✅ hecho | integrations-n8n | GHL API v2. PUSH y PULL probados (con contactos dummy, limpiados) |
| PROSP-1 | **Módulo de prospección con avatar asistido por IA** | 🟡 código listo, **falta aplicar migración + probar en vivo** | web-admin / backend-supabase | Rama `feat/prospeccion-ia`. Nueva sección **Prospección** en el panel: chat con un agente (Claude) que ayuda a definir el avatar y **recomienda** filtros → búsqueda contra **Google Places API (New)** → resultados en pantalla **sin persistir** → botón "Migrar a Supabase" guarda los seleccionados en `prospects` → RPC `promote_prospects` los pasa a `clients`. **GHL deliberadamente fuera**: los clientes nacen con `origin='hunter'` y `push_to_crm()` solo empuja `origin='app'` (D13). Migración `0028_prospects.sql` (tablas `prospects` + `prospect_searches`, RLS por creador, RPCs `prospect_import_status` y `promote_prospects`, CHECK de `origin` extendido, guard anti-spam en `notify_lead_assigned`). Código: `web/src/lib/prospect/*` (tipos, packs de nicho, scoring puro, cliente Places, agente), rutas `/api/prospect/{chat,search}`, página `/prospeccion`, componentes en `components/prospeccion/`. Sin `ANTHROPIC_API_KEY` degrada a modo guiado (D15). Verificado: `tsc` limpio, `eslint` limpio, `next build` OK con las 3 rutas nuevas. **Falta**: (a) aplicar `0028` en Supabase Cloud y correr `get_advisors`, (b) cargar `GOOGLE_PLACES_API_KEY` (y opcionalmente `ANTHROPIC_API_KEY`) en el entorno de la web, (c) **probar en vivo** — no se ejecutó ninguna búsqueda real contra Places. Ver `docs/PROSPECCION.md` |
| PROSP-3 | **Asistente sobre OpenRouter + API keys desde Configuración** | 🟡 código listo, falta aplicar migración | web-admin / backend-supabase | Migración `0029_ai_provider_settings.sql`. El agente dejó de usar el SDK de Anthropic y llama a OpenRouter (API compatible con OpenAI, tool calling + respaldo por bloque JSON). Nueva sección **Configuración → Prospección**: API key de OpenRouter, modelo (libre, default `openrouter/auto`), interruptor del asistente y API key de Google Places. Las keys van a `private.integration_secrets` vía RPC; leerlas está concedido **solo a `service_role`** (D17), así que la web necesita `SUPABASE_SERVICE_ROLE_KEY` — con fallback a variables de entorno. Verificado: tsc/eslint/build limpios. **Falta**: aplicar `0029`, cargar las keys y probar un turno real de chat |
| PROSP-5 | **Enriquecimiento de prospectos con Instagram (Apify)** | 🟡 código listo, falta aplicar migración | web-admin / backend-supabase | Migración `0030_prospect_instagram_enrichment.sql`: columnas `ig_*` en `prospects` + `ig_activity` (activo/tibio/dormido según la última publicación) + token de Apify en la allowlist de secretos. Nuevo paso opcional **después** del guardado: botón "Enriquecer con Instagram" → `/api/prospect/enrich` → actor `apify/instagram-profile-scraper` (lote de hasta 25 perfiles por run). Resuelve que la búsqueda detectaba el handle pero no si la cuenta estaba viva. **El score NO se recalcula** a propósito: la señal nueva vive en `ig_activity`. Token configurable en Configuración → Prospección. Verificado: tsc/eslint/build limpios. **Falta**: aplicar `0030`, cargar el token y probar un run real |
| PROSP-7 | **Prospección: arreglo en móvil, flujo numerado, LATAM y más rubros** | ✅ código listo (2026-08-14); ⏳ falta confirmar en un teléfono real | web-admin | Pedido del usuario tras entrar por primera vez al panel. **(1) Bug en móvil** — reportó que en el chat de Turbo no podía escribir ni seleccionar, y que la barra espaciadora no respondía. **No se pudo reproducir**: en escritorio el campo funciona (probado en el deploy, espacios incluidos) y las herramientas de esta sesión no lograron emular el viewport de un teléfono. Se atacaron las dos causas conocidas que dan ese cuadro: (a) `Field.tsx` usaba `text-sm` (14 px) — **por debajo de 16 px Safari en iOS hace zoom solo al enfocar**, la página salta y el campo se va de pantalla; ahora es `text-base` en móvil y `sm:text-sm` en escritorio, con alto `h-11` (44 px, el mínimo táctil recomendado) y `sm:h-9`. Los dos `<textarea>` de `FiltersPanel` duplicaban ese estilo a mano y quedaban afuera del arreglo: corregidos también. (b) `AvatarChat` envolvía todo en `h-full`, pero **el padre no define altura**, así que se resolvía como `auto` y el historial crecía sin techo, empujando el campo de escritura fuera de la pantalla; ahora el alto es fijo (`h-[26rem] sm:h-[30rem]`) y el que scrollea es el historial, con `overscroll-contain`. Chips de sugerencia con `py-2` en móvil (antes ~24 px de alto, se erraba el toque). **(2) Flujo numerado** — las tres tarjetas parecían independientes; ahora se leen como secuencia (`1 · Definí el avatar`, `2 · Filtros`, `3 · N candidatos`) con un rótulo guía arriba. Importa sobre todo en el teléfono, donde se ven apiladas. **(3) LATAM** — `COUNTRIES` pasa de 6 a 20 países: se suman PE, EC, BO, PY, VE, BR, CR, PA, GT, SV, HN, NI, DO y PR. Donde el móvil no se distingue del fijo por prefijo (DO y PR, plan de EE.UU.) el patrón queda en `null` a propósito y la interfaz avisa que esa señal no filtra. **(4) Rubros** — de 4 packs a 15: belleza (peluquerías y barberías, que `estetica` excluye por nombre), salud, fitness, automotor, construcción, veterinarias, educación, comercio, eventos, turismo y hogar. Cada uno con sus términos, portales que no cuentan como web propia y exclusiones; los pesos de scoring se ajustaron por rubro (verificado que los 15 suman 100). Los selectores ya eran dinámicos, así que aparecen solos |
| UX-6 | **Revisar el uso del verde en las tarjetas** | pendiente | web-admin | Pedido del usuario (2026-08-15), como continuación de lo hecho en el chat. Ya se separó el mint de acción del mint de texto (D24) y eso bajó la carga general, pero **las tarjetas no se revisaron una por una**: `StatCard` (ícono en `--badge-primary-bg`), `Badge` tone primary, filas seleccionadas en `bg-primary/5`, el chip "sin web" de `ResultsTable` y el `ProgressBanner`, que es la superficie más verde de la app. La regla a aplicar es la del manual: el verde marca acción, progreso y foco; el resto respira. Conviene mirarlo con datos reales en pantalla, no leyendo clases: el problema anterior no se veía en el código, se veía al usar la app |
| UX-7 | **Mejorar UX/UI en general (segunda pasada)** | pendiente | web-admin | Pedido abierto del usuario (2026-08-15). Distinto de UX-1…UX-5, que son de julio y anteriores al rebranding: esto es una segunda pasada **ya con la identidad ST Labs puesta y la app en uso real**. Sin alcance cerrado todavía; conviene que el usuario marque las pantallas que más le molestan al usarlas, en vez de auditar a ciegas. Candidatas conocidas: las que nunca se vieron con sesión real (Inicio, Clientes, Equipo, Reportes, Contactos GHL) |
| PROSP-8 | **Revisar el flujo completo de comunicación con Turbo** | pendiente | web-admin | Pedido del usuario (2026-08-15). **El chat nunca funcionó end-to-end**: recién el 2026-08-15 se destrabó el header que rompía toda llamada a OpenRouter (ver 0h en STATE), y después de eso no se pudo probar un turno real. Hay que recorrer el circuito entero: mensaje → `/api/prospect/chat` → OpenRouter → propuesta de filtros → que los filtros lleguen bien al panel → buscar. Verificar además los caminos de error: sin key (modo guiado), sin saldo, modelo inexistente, y el tope de 60 s que ahora declara la ruta. Ojo con dos cosas ya conocidas: el fallback por bloque ```json cuando el modelo no soporta tool calling (D16), y que el modelo se configura en Configuración → Prospección (vacío = `openrouter/auto`) |
| PERM-4 | ~~Que el superadmin pueda ver el sistema como vendedor y como lector~~ | ❌ descartado por el usuario (2026-08-15) | — | Se pidió y se descartó el mismo día, al confirmar que **la web ya tiene una sola interfaz que se adapta al rol** (el `/vendedor` separado se eliminó en el Sprint 3): el superadmin ya ve todas las secciones, así que para navegar y controlar no falta nada. **Queda anotado lo que sigue sin cubrir, por si reaparece**: ver todas las secciones no es lo mismo que ver *cómo experimenta el sistema* otro rol. El caso concreto es **Inicio**, que tiene dos vistas distintas bajo la misma ruta (`page.tsx` ramifica en `role === 'seller'`): el superadmin ve el panel de métricas y **nunca** ve el banner de progreso ni "Mis pendientes" del vendedor. Si algún día un vendedor reporta un problema en su Inicio, el superadmin no puede reproducirlo sin entrar con una cuenta de vendedor. Y si se retoma, vale el análisis original: la simulación por interfaz es barata (todo se decide leyendo `profile.role` en el componente) pero **incompleta**, porque el RLS filtra por el usuario autenticado y no por lo que muestre la UI — se vería la pantalla del vendedor con los datos del admin |
| PROSP-9 | **Exportar los resultados de Turbo sin cargarlos como clientes** | ✅ hecho (2026-08-16) dentro de `PROSP-12` Fase 4. Botón **"Exportar a Excel"** sobre los resultados en pantalla, con nombre, calificación en palabras, motivos, zona, WhatsApp, Instagram, LinkedIn, web, rating y ficha. Sale del **estado en memoria** y no de la base, porque los resultados no están persistidos hasta que se guardan (D14) — exportar tiene que servir justamente para lo que se decidió NO guardar. **De paso**: el CSV pasó de coma a **punto y coma**, porque Excel en español abre un CSV con comas entero en una sola columna; se arregló en `reportes/ExportButton.tsx`, que es compartido, así que **Reportes tenía el mismo problema y quedó corregido también** | web-admin | Pedido del usuario (2026-08-15). Hoy el único destino de una búsqueda es "Migrar a Supabase" y después promover a `clients`: si solo querés la lista para trabajarla afuera, no hay salida. Falta un **exportar a CSV** sobre los resultados en pantalla (y también sobre los prospectos ya guardados), con los campos que ya trae `ProspectResult`: nombre, dirección, zona, teléfono, WhatsApp, web, Instagram, rating, score y link a Maps. Reusar el patrón de `reportes/ExportButton.tsx`, que ya arma y descarga CSV. Ojo: los resultados **no están persistidos** hasta que se guardan (D14), así que el export tiene que salir del estado en memoria de la corrida |
| PROSP-4 | Rate limit / tope de gasto por usuario en prospección | pendiente | web-admin | Hoy cualquier vendedor puede lanzar búsquedas sin límite y cada una son hasta 24 consultas facturadas a Places. Falta un tope por usuario/día (contando sobre `prospect_searches`, que ya registra cada corrida) y, en Google Cloud, un presupuesto con alerta |
| PROSP-6 | **Enriquecimiento de contacto de prospectos (email + WhatsApp)** | 🟢 Fase 0 hecha y aprobada (2026-08-14); fases 1-4 pendientes | web-admin / backend-supabase | Plan completo por fases en [`docs/PROSPECCION-CONTACTOS.md`](../docs/PROSPECCION-CONTACTOS.md). **El problema**: `prospects` no tiene columna `email` y `promote_prospects` no lo copia — **todo lead de prospección le llega al vendedor sin email**, aunque `clients.email` existe desde la `0001`. Places nunca da el email: da el sitio web. **Fase 0 (hecha)**: se validó el actor [`vdrmota/contact-info-scraper`](https://apify.com/vdrmota/contact-info-scraper) contra 5 sitios reales del ICP (3 inmobiliarias + 2 clínicas, AR) antes de escribir código. Resultado: **email en 4 de 5 (80%)**, muy por encima del 40-60% estimado; **WhatsApp en 4 de 5**; 38 s la corrida entera; **costo real $0.024 / 12 páginas ≈ $0.005 por sitio** (~$0.50 cada 100 prospectos). El que falló (`clinicarobles.com`) devolvió 0 páginas: el sitio bloquea al scraper — se resolvería con `useBrowser`, pero encarece todas las corridas por 1 de cada 5, así que queda apagado. **Hallazgo que amplía el alcance**: la misma corrida trae WhatsApp y redes sin costo extra. El WhatsApp vale tanto como el email (es el canal principal del vendedor y el score lo premia) y las redes permiten completar el `instagram` que Places no detectó, habilitando PROSP-5 sobre esos prospectos. Por eso las fases siguientes son "enriquecimiento de contacto", no solo email. **Falta**: migración `0031` (columna `email` + control + `email` en `promote_prospects`), `lib/prospect/contacts.ts`, ruta `/api/prospect/enrich-contacts`, botón en la UI y la prueba punta a punta (buscar → guardar → enriquecer → promover → que el email llegue a la ficha). No recalcula el score, igual que D18 |
| PROSP-2 | **Pantalla de prospectos guardados (`/prospeccion/guardados`)** | ✅ código listo (2026-08-15, PR #15); ⏳ `0033` aplicada (2026-08-15); falta recorrerla con sesión real | web-admin | **Lo reportó el usuario como bug**: hizo una búsqueda, guardó 50 leads y no los vio nunca más. Los prospectos vivían en `useState` del estudio y ninguna pantalla leía la tabla `prospects`, así que al recargar quedaban en la base pero inalcanzables — y sin verlos tampoco había forma de asignarlos. Ruta nueva, **subruta de `/prospeccion` a propósito**: no agrega un `SectionId`, así que no toca la matriz de permisos ni `sections.ts`. El RLS de `prospects` (`0028:116`) ya recorta, misma query para vendedor y superadmin, **sin tocar policies**. Acciones sobre la selección: asignar (reusa el RPC `promote_prospects` que ya existía), enriquecer con Instagram y descartar. **Descartar es UPDATE y no DELETE** aunque la policy de delete exista: borrar liberaría el `google_place_id` y el negocio reaparecería como nuevo, rompiendo el dedupe. **La otra mitad del arreglo es que se sepa que existe**: pestañas Buscar/Guardados con contador traído del servidor (es lo que hace que en una sesión nueva se vea "Guardados (50)" sin haber buscado nada), toast con acción "Ver guardados" al guardar, y link permanente en la tarjeta de la corrida. **Migración `0033`**: promover 50 prospectos insertaba 50 clientes ya asignados y disparaba **50 notificaciones** — el guard anti-spam de `notify_lead_assigned` solo cubría `origin='ghl'`. Se invierte: al INSERT solo notifica lo cargado a mano (`origin='app'`), así el próximo origin importado hereda el criterio; las reasignaciones (UPDATE) siguen notificando siempre. **Fix de paso**: `/api/prospect/enrich` cortaba en 25 con un `.limit()` en la consulta — seleccionar 50 enriquecía 25 y el resto desaparecía sin rastro; ahora devuelve `overflow` y la UI lo dice. También `lib/sellers.ts` unifica la consulta de vendedores que estaba duplicada en `clientes/` y `prospeccion/`. **Verificado**: tsc limpio, build verde con la ruta en el listado, lint sin problemas nuevos. **Falta**: aplicar la `0033` y probar el recorte por RLS entre vendedor y superadmin |
| PROSP-10 | **La cantidad de resultados pedida no se respetaba** | ✅ código listo (2026-08-15, PR #14) | web-admin | Dos bugs reportados por el usuario que son el mismo camino de datos. **(1) "Puse máximo 2 y me trajo 5"**: había un piso duro de 5 en `clamp(limit, 5, 60)`, repetido en la API, en el agente y como `min={5}` en el input; cualquier valor menor se subía en silencio. No había razón técnica para ese 5. **(2) "Le digo a Turbo que busque 2 y el filtro queda en 30"**: el campo `limit` estaba declarado **sin `description` y fuera de `required`**, y el prompt no mencionaba la cantidad en ningún lado, así que omitirlo era el camino de menor esfuerzo para el modelo y caía siempre en el default. Ahora hay una sola fuente de verdad (`MIN_LIMIT`/`MAX_LIMIT`/`DEFAULT_LIMIT` + `clampLimit()` en `lib/prospect/types.ts`, módulo **puro** sin `server-only` para que lo pueda importar también el panel) y el contrato del agente exige el campo explicando qué poner cuando no se pidió nada. **De paso, ahorro real**: la recolección corta al juntar 5× el límite pedido (piso 40), en el borde de zona/query — se corta **por tamaño de pool y no por `limit`**, porque cortar en 2 devolvería los dos primeros que pasaron el filtro y no los dos mejores. Con el default el objetivo queda en 150 y no se alcanza: las búsquedas normales no cambian. **Verificado**: tsc, build, lint sin problemas nuevos y 12 casos de `clampLimit` ejercitados (`0 → 1`, antes `→ 5`). ⚠️ **El modo guiado sigue ignorando la cantidad pedida por texto** (sin API key arma los filtros con heurísticas y no lee la intención) |
| PROSP-11 | **El LinkedIn detectado no se veía en ninguna parte** | ✅ código listo (2026-08-15, PR #17); `0035` aplicada (2026-08-15) | web-admin / backend-supabase | Pedido del usuario: *"si el usuario pide datos de LinkedIn debe aparecer LinkedIn"*. PROSP-LI (PR #12) sumó LinkedIn como **señal de filtro** y lo guardaba en la base, pero **no lo mostraba en ningún lado**: ni en la tabla de resultados de la búsqueda ni en la de guardados. Se podía exigir la señal y después no había forma de confirmar qué había encontrado. **Bug de fondo**: `extractLinkedin` descartaba si el perfil era `company/`, `in/` o `school/` y guardaba solo el slug — con eso **la URL no se puede reconstruir** (`company/acme` e `in/acme` son perfiles distintos y no hay forma de adivinar cuál era). Se corrigió para guardar el tipo incluido (`company/acme`); se pudo cambiar sin migración de datos porque la `0031` todavía no está aplicada y no hay ni una fila guardada. Helpers `linkedinUrl()` y `linkedinLabel()` en `types.ts` y no en `places.ts`, que es `server-only` y no lo puede importar la tabla. La columna en guardados **solo aparece si hay al menos un LinkedIn**: en la mayoría de los rubros Places no lo publica y sería una columna de guiones. **Migración `0035`**: `promote_prospects` armaba las notas del cliente con Instagram y ficha de Google y **se olvidaba el LinkedIn**, así que el vendedor podía filtrarlo, verlo, y no recibirlo cuando el lead le llegaba — el mismo agujero que PROSP-6 describe para el email. Va como URL completa y no como slug, porque la nota la lee una persona. **Verificado**: tsc, build, lint sin problemas nuevos, y 17 casos del extractor incluida la **ida y vuelta** (extraer → guardar → rearmar URL → volver a extraer da lo mismo), que es la propiedad que antes no se cumplía |
| WEB-30 | **Invitaciones que no llegan y no se pueden reenviar** | ✅ código listo (2026-08-15, PR #16); `0034` aplicada y función desplegada (2026-08-15); ⏳ falta confirmar `PUBLIC_SITE_URL` + Redirect URL y probar el enlace | web-admin / backend-supabase | **Lo reportó el usuario**: invitó a alguien, el email nunca llegó y el sistema no lo dejaba reenviar. Eran **tres fallas encadenadas**. (1) **Se perdía el botón al primer intento**: invitar crea el usuario en `auth.users`, el trigger le crea el perfil, y "invitación pendiente" estaba definido como *email autorizado sin perfil* (`equipo/page.tsx:30`) — con perfil ya creado la persona **se caía de esa lista** y pasaba a "Vendedores", donde no había botón de reenviar. (2) **Aunque lo tuviera, no reenviaba**: `inviteUserByEmail` no reenvía a usuarios existentes, y la función devolvía `{ok:true, alreadyRegistered:true}` con **200 sin haber mandado nada**. (3) **El error real nunca se mostraba**: el detalle viaja en `error.context` y se descartaba, y encima todo se informaba con `toast.success` — si el envío se cortó por el tope de emails por hora del remitente compartido, el superadmin no se enteraba. **Solución**: la función acepta `mode: 'link' | 'send'`; en `'link'` usa `generateLink` y **devuelve el enlace sin mandar correo**, para pasarlo por WhatsApp; en `'send'`, si el usuario ya existe manda un magic link con `signInWithOtp`. **El enlace se genera siempre del lado del servidor**: el cliente del browser usa PKCE y guardaría el `code_verifier` en la máquina del admin, así que el invitado recibiría un `?code=` sin verificador. Nuevo `MemberAccessActions` (Copiar enlace · WhatsApp · Reenviar email) en **las cuatro listas**, no solo en pendientes — eso es lo que destraba el caso reportado. Errores honestos vía `components/equipo/invite.ts`, que lee `error.context` y traduce `over_email_send_rate_limit`. **Migración `0034`**: RPC `member_access_status()` para el badge **"Nunca ingresó"** — `last_sign_in_at` vive en `auth.users`; el chequeo de superadmin va como `where` y no como `raise` para que devuelva vacío en vez de romper la página. Esa señal es lo que hacía el problema invisible: el invitado figuraba como vendedor activo. **Bug encontrado de paso**: el `redirectTo` por defecto de la función apuntaba a `crmlite.moremigracion.com`, **el dominio de la marca anterior** — quedó de la migración de identidad; ahora sale de `PUBLIC_SITE_URL` y se rechaza cualquier destino externo. **Verificado**: tsc, build, lint sin problemas nuevos. **Falta**: `supabase functions deploy invite-user`, aplicar la `0034`, cargar `PUBLIC_SITE_URL` y tener el destino en Redirect URLs. ⚠️ El tope de emails del remitente compartido **sigue existiendo**: este PR hace que se vea, no lo elimina — el fix real es SMTP propio |
| PROSP-12 | **Turbo como cerebro multi-fuente (Maps · LinkedIn · Instagram · TikTok)** | ✅ **7 fases implementadas (2026-08-16)**; migraciones `0036`→`0038` aplicadas y verificadas; ⏳ **falta probar contra los proveedores reales** (ver abajo) | web-admin / backend-supabase | Plan completo por fases en [`docs/PLAN-TURBO-MULTIFUENTE.md`](../docs/PLAN-TURBO-MULTIFUENTE.md). **Pedido del usuario**: que Turbo deje de llenar un formulario de Places y pase a ser un experto en ventas que entiende qué vende el usuario, **elige la fuente** donde están esos clientes, **muestra el plan antes de gastar** y recién con el visto bueno ejecuta. **Por qué no alcanza con mejorar el prompt** (medido, no supuesto): la instrucción de Turbo dice textualmente que la búsqueda corre contra Places; los filtros son conceptos de Maps (zona, rating, "tiene web propia") y LinkedIn no tiene ninguno; `prospects.google_place_id` es **not null unique** y una persona de LinkedIn no tiene uno (ya hubo que falsificarlo para el Excel: `xlsx:ticket1500:…`); el scoring usa fotos/reseñas/rating, así que una persona puntuaría 0. **Medición del puente actual**: de 47 prospectos de Turbo solo **17 tienen Instagram** y **0 tienen LinkedIn** sobre 166 totales — el handle se extrae únicamente del campo "sitio web" de la ficha de Google, así que **el filtro "requiere LinkedIn" que se construyó en PROSP-11 devuelve cero siempre**. **Precios verificados (ago-2026)**: Places US$ 40/1.000 consultas (1.000 gratis/mes) ≈ **US$ 0,96 por corrida**; Instagram US$ 1,60–2,60/1.000 perfiles ≈ **US$ 0,24 los 92 pendientes**; LinkedIn `harvestapi` US$ 0,10 por página de 25 (elegido **por no pedir cookies**: los baratos usan la sesión del usuario y pueden hacer que le restrinjan la cuenta); TikTok US$ 0,30/1.000. **Descubrir cuesta 4× más que verificar, y hoy racionamos lo barato**. **7 fases**: 0) deuda de Apify (campos ya pagados que se tiran: web de la bio, verificado, rubro, seguidos; topes nativos `maxItems`/`maxTotalChargeUsd`; 408 vs 402) + ensanchar el puente con `vdrmota/contact-info-scraper`, **el actor ya validado en PROSP-6 Fase 0** (80% de acierto, US$ 0,005/sitio, trae email + WhatsApp + redes); 1) migración **`0036`** — `source`+`source_ref` únicos en conjunto reemplazando a `google_place_id`, `kind` (negocio/persona/cuenta), campos de persona, `email` (que PROSP-6 necesita), `audience_size`/`audience_activity` genéricos, `source_data` jsonb, tabla `prospect_runs`, `plan`+`approved_at` en `prospect_searches`. **Verificado contra la base: `prospect_import_status` es la única función que menciona `google_place_id`**; 2) abstracción de "fuente" (puro refactor, verificable porque el scoring es puro); 3) **ejecución asíncrona** — arregla el tope de **60 s del plan Hobby** (hoy `/api/prospect/enrich` declara 300, ver WEB-29) y la trampa del **408 de Apify que factura sin devolver nada**; se descartan los webhooks de Apify porque necesitan una Edge Function y **esta cuenta da 403**; 4) **Plan de Caza** (a quién · dónde y por qué · cuántos · cuánto cuesta · cuánto tarda → Aprobar/Editar/Otra opción) + **pantalla legible**: "Score" pasa a "Calificación" con palabra y **razones visibles** (hoy se calculan y viven en un tooltip que en el teléfono no existe) + exportar a Excel, que **cierra PROSP-9**; 5) Turbo multi-herramienta (una por fuente) + fijar modelo en vez de `openrouter/auto`; 6) LinkedIn como fuente real (**mismo token de Apify, sin credenciales nuevas**); 7) IG/TikTok como descubrimiento + acercamiento asistido a pedido. **Autonomía verificada antes de escribir el plan**: se pueden aplicar migraciones por el pooler (como la 0033-0035) y **probar contra las APIs reales** — las tres claves están en `web/.env.local`. Ninguna fase necesita Edge Functions ni variables nuevas en Vercel. Lo único que requiere al usuario es **mergear un único PR al final**. Costo estimado de todas las pruebas con lotes chicos: **menos de US$ 2**. **Riesgos anotados**: raspado contra los términos de IG/TikTok/LinkedIn (mitigado con el actor sin cookies), datos personales de LinkedIn (camino de borrado contemplado en la Fase 1), y **el dedupe entre fuentes queda sin resolver a propósito** (cruzar identidades por nombre da falsos positivos peores que un duplicado). ⚠️ **RESULTADO DE LA EJECUCIÓN (2026-08-16, rama `feat/turbo-multifuente`)**: las 7 fases están hechas, pero **la afirmación del plan de que se podía probar contra las APIs reales era FALSA** — las tres claves están declaradas pero **vacías** en `web/.env.local` (el chequeo original listó los nombres de las variables sin mirar si tenían valor), así que **no hubo ni una sola llamada a Google, OpenRouter o Apify**. Lo verificado de verdad: migraciones aplicadas en producción y comprobadas **por comportamiento** (insertar y revertir, no solo mirar que la columna exista), **54 tests** nuevos con el runner nativo de Node (`npm test`, se agregó porque **el proyecto no tenía ninguno**), `tsc` limpio, `next build` verde con las 7 rutas, `eslint` sin un solo problema en los archivos nuevos, RLS activo en las 3 tablas y `search_path` fijo en las 31 funciones `security definer`. **Dos desvíos del plan**: (1) la Fase 1 se hizo **antes** que la 0, porque los campos nuevos de Apify y el email necesitan columnas donde guardarse; (2) **TikTok quedó afuera a propósito** (mercado más chico para B2B, nadie lo pidió) — el catálogo lo describe para que la decisión se vea, pero no tiene ejecutor. **Bug encontrado por la propia verificación**: la `0036` puso `DEFAULT` en `source`/`kind` **y** un trigger que los deriva; Postgres aplica los DEFAULT **antes** de los BEFORE triggers, así que la derivación era código muerto y una persona de LinkedIn se guardaba como `business` — corregido en la `0037`. **Falta**: cargar las 3 claves en `web/.env.local` y correr las 7 pruebas listadas en la sección 9 del plan |
| PROSP-13 | **Turbo entrevistador: LinkedIn en cero, señales que elige Turbo, y el chat usable** | ✅ código listo (2026-08-17, PR #24) | web-admin | **Lo reportó el usuario**: buscar en LinkedIn devolvía **siempre 0** con el cartel "aflojá las señales exigidas". **No era un filtro**: la documentación del actor `harvestapi/linkedin-profile-search` **no coincide con lo que devuelve**. Verificado con una corrida real: en modo `Short` manda `id, linkedinUrl, firstName, lastName, summary, currentPositions[], location{linkedinText}`; la doc prometía `publicIdentifier`, `headline` y `currentPosition` (singular), y **ninguno existe**. Como el mapeo usaba `publicIdentifier` como identidad, **descartaba todos los perfiles**. Corrida posterior: **5 de 5 mapeados** con cargo, empresa y antigüedad. Entra además la **antigüedad en el cargo** (`tenureAtPosition`), la señal que faltaba para puntuar personas y que venía gratis. **Bug extra encontrado por los tests**: el id de LinkedIn **distingue mayúsculas** y se pasaba a minúsculas — la URL guardada no llevaba a ese perfil. **El puntaje ordena y ya no filtra**: `minScore` era la segunda forma de llegar a cero en silencio (Turbo ponía 35, calibrado con las fotos y reseñas de Google, aplicado también a LinkedIn donde la escala mide otra cosa); se sacó de las tres fuentes, del panel y del contrato del agente, y el corte lo da `limit`. **`requireNoWebsite` deja de ser un default fijo** y lo deriva Turbo de la oferta — verificado en real: lo activa para "hago páginas web" y no para "vendo insumos descartables". El **Plan de Caza** muestra las señales como frases con un "sacar" al lado, y solo las de la fuente que corresponde. **Cero resultados se explica**: en vez de pedir que el usuario adivine, dice qué señal se llevó más puestos y ofrece sacarla (edita el filtro, no vuelve a buscar solo, porque buscar cuesta plata). **La entrevista**: Turbo pregunta primero por la oferta y el dolor, avanzando con hipótesis en vez de interrogar, y **la oferta se guarda y se reusa** en el primer mensaje a cada prospecto. **El chat**: el cuadro de texto era un `<input>` de una línea con alto fijo (cuanto más escribías menos veías); ahora crece hasta 6 líneas con Enter para enviar y Shift+Enter para saltar. Emojis con moderación (uno por mensaje, solo cuando aporta) y **opciones clickeables** vía herramienta `preguntar` — al tocarlas se envía ese texto, no ejecutan nada, así ningún toque gasta plata. **Tercer bug de tokens**: los modelos que razonan se comen el presupuesto y la última frase llega partida; ahora se recorta a la última frase completa. **Verificado**: 73 tests (16 nuevos, incluida la forma REAL del ítem de LinkedIn), tsc, build y **cero lint** en lo tocado, más corridas reales contra LinkedIn y OpenRouter por menos de US$ 1. ⚠️ **Arrastra el commit `2d94a98`**, que quedó afuera del #23 al mergearse antes de pushearlo: hasta que #24 entre siguen vivos en producción tres bugs ya arreglados. **Falta**: teléfono real, búsquedas en segundo plano y el circuito completo hasta que el email llegue a la ficha |

## 🟡 Normal

| ID | Tarea | Estado | Agente | Notas |
|---|---|---|---|---|
| EDGE-1 | Desplegar/ver Edge Functions (`sync-ghl`, `send-whatsapp`) en el proyecto cloud (si no se va 100% a n8n) | pendiente | backend-supabase | Hoy solo estaban en el self-hosted |
| WEB-1 | **Web admin** (Next.js): Login+gate, Inicio, Equipo, Clientes (filtros+CSV+reasignar), Contactos GHL, Reportes | ✅ hecho (v1) | web-admin | En `web/`. Falta: deploy + allow-list localhost |
| WA-1 | Activar **WhatsApp Business API** (el switch ya existe) | pendiente | mobile-app / backend-supabase | Requiere número aprobado |
| PROD-1 | Producción: `SITE_URL` → `crmlite://auth-callback`, build EAS, publicar en tiendas | pendiente | mobile-app | Ver STATE datos clave |
| MOB-1 | Probar a fondo todas las funciones de la app en el proyecto cloud | pendiente | mobile-app | Base cloud está limpia |

## ⚪ Ideas / futuro

| ID | Tarea | Estado | Agente | Notas |
|---|---|---|---|---|
| IDEA-1 | Meta diaria configurable por el superadmin desde la app | pendiente | mobile-app | `app_settings.daily_goal` ya existe |
| IDEA-2 | Soportar más CRMs en n8n (HubSpot, Pipedrive) | pendiente | integrations-n8n | Solo agregar flujos n8n |
| IDEA-3 | Notificaciones push de recordatorio de seguimiento | pendiente | mobile-app | |

---

## 🚀 Roadmap de mejoras (propuestas 2026-07-08)

> Lluvia de ideas para priorizar. Borrá/reordená lo que no aplique.
> Prioridad: 🔴 desbloquea · 🟠 alto valor · 🟡 mejora. (Algunas se cruzan con ideas ya listadas arriba.)

### Web (panel)
| ID | Tarea | Prioridad | Notas |
|---|---|---|---|
| WEB-2 | Deploy real (Dokploy en servidor nuevo) + dominio | 🟠 en curso | Docker listo (`web/Dockerfile` + compose, build verificado local). Falta: servidor con Dokploy + DNS → ver `docs/MIGRACION-SERVIDOR.md` |
| WEB-29 | **Preparar el panel para desplegar en Vercel** | ✅ código listo (2026-08-14); ⏳ falta crear el proyecto en Vercel | Guía completa en [`docs/DEPLOY-VERCEL.md`](../docs/DEPLOY-VERCEL.md). **Cambios de código**: (1) `next.config.ts` — `output: 'standalone'` estaba fijo para Docker y Vercel no lo quiere; ahora se activa solo fuera de Vercel (detecta la env `VERCEL`, sin configurar nada), así **Docker y Vercel conviven** sin elegir uno. Verificado los dos modos: con `VERCEL=1` no genera `.next/standalone`, sin ella sí genera `server.js`. (2) Nuevo `lib/supabase/env.ts` — las 3 formas de crear el cliente (browser / server / middleware) usaban `process.env.X!`, así que un deploy sin variables fallaba con un "Invalid URL" indescifrable; ahora dice qué falta y dónde cargarlo. Ojo: **el build sale verde igual sin las variables** (las páginas que las usan son dinámicas), el error recién aparece al abrir el sitio. **Pendiente del usuario**: crear el proyecto en Vercel con **Root Directory `web`** (el repo es multi-carpeta: sin eso falla con "No Next.js version detected"), cargar las variables, y **actualizar Site URL + Redirect URLs en Supabase y los orígenes en Google OAuth** — sin eso no hay login y las invitaciones caen en `localhost`. ⚠️ **Límite de plan**: `/api/prospect/enrich` declara `maxDuration = 300`, que **excede el tope de 60 s del plan Hobby**; hay que bajarlo a 60 o usar Pro (no se puede hacer configurable: Next exige un literal) |
| WEB-3 | Commit + push de todo el trabajo | 🔴 | nada está en git todavía |
| WEB-4 | Acciones en masa (reasignar / estado / borrar) | ✅ hecho | web-admin | Checkbox + barra asignar/estado/borrar (2026-07-09) |
| WEB-5 | Importar CSV mejorado (preview, dedup, plantilla) | 🟠 parcial | Preview + dedup + plantilla (2026-07-09). Falta: mapeo columnas custom |
| WEB-6 | Dashboard: tendencia por día + feed de actividad reciente | ✅ hecho (2026-07-17) | Inicio admin/viewer (`app/page.tsx`) ahora tiene: (1) fila "Esta semana" con 2 StatCards de flujo (Nuevos, Contactos) con **delta vs. semana anterior**; (2) `TrendChart.tsx` (nuevo, recharts AreaChart 2 series — clientes nuevos + contactos por día, últimos 30 d, rellena días vacíos con 0, tokens del tema); (3) `ActivityFeed.tsx` (nuevo — feed de clientes agregados/contactados/ganados, más nuevo primero, fecha relativa). Agregación en JS (sin migración): trae `clients` + `interactions` de los últimos 35 d en paralelo. `StatCard` extendido con `delta`/`deltaLabel` opcionales; `formatRelativeTime()` nuevo en `format-dates.ts`. "Ganados" no entra en la línea de tendencia (sin timestamp confiable, `updated_at` es aproximación en el feed). Ver también UXR-6. `tsc` limpio + `next build` verde (todas las rutas). **Sin probar en navegador real** (requiere login Google) |
| WEB-7 | Badges de "pendientes"/"vencidos" en el sidebar | ✅ hecho (2026-07-18) | `SidebarNav` muestra un `Badge` junto a "Inicio" (pendientes, tono warning) y "Clientes" (vencidos, tono danger), solo si el contador > 0. `AppShell` (client) trae los contadores con el client supabase (2 count queries, el RLS recorta por rol: vendedor cuenta los suyos, admin/viewer todos) y los pasa a ambas instancias del sidebar (desktop + drawer). Ver UXR-7 para el dashboard |
| WEB-8 | Paginación / virtualización de la tabla de clientes | ✅ hecho (2026-07-11) | Antes traía y dibujaba los 46 clientes de una — `ClientsTable.tsx` ahora pagina de a 20 (`visible = filtered.slice(0, visibleCount)`, tarjetas móvil y tabla desktop) con botón "Cargar más". La selección múltiple ("Seleccionar todos los filtrados") sigue operando sobre todo lo filtrado, no solo lo paginado — no se perdió esa función. Motivado por WEB-26 (ver esa fila). `tsc`/`build` OK, **sin probar en el celular real del usuario todavía** (requiere login Google real, no se pudo probar interactivo en esta sesión) |
| WEB-9 | Exportar clientes a CSV | 🟠 | |
| WEB-10 | Búsqueda global (Cmd/Ctrl+K) | 🟡 | |
| WEB-11 | Vistas / filtros guardados | 🟡 | |
| WEB-12 | Rol "supervisor" (entre vendedor y superadmin) | 🟡 | |
| WEB-13 | Asignación automática (round-robin) al importar/traer GHL | 🟡 | |
| WEB-14 | Auditoría (log de cambios de estado/asignación) | 🟡 | |
| WEB-15 | Logo desde el panel (subir a Storage) | 🟡 | hoy es archivo en el repo |
| WEB-16 | PWA · i18n · tests (Playwright) | 🟡 | |
| WEB-17 | **Convertir vendedor existente en administrador** | ✅ hecho (2026-07-10) | RPC `set_user_role` endurecida (migración `0014_set_user_role_guard.sql`: impide que un admin se autodegrade y evita dejar el sistema sin ningún superadmin, probado con transacción revertida en Supabase). Panel Equipo: botón "Hacer admin" en Vendedores y "Bajar a vendedor" en Administradores, con confirmación inline. Caso puntual `soporte@justmore.net` ya resuelto a mano por SQL el 2026-07-10 |
| WEB-26 | Investigar glitch visual (estática/ruido de colores) al entrar a `/clientes` en mobile real | 🟠 diagnóstico revisado con video (2026-07-11) | Confirmado por el usuario en vivo (celular real), pasa apenas entra. Se descartó: blur sin proteger (`md:backdrop-blur`), hydration mismatch, FOUC de tema. **Primera hipótesis (2026-07-11, sin video)**: `color-mix()` de Tailwind v4 sobre tokens oklch en `Badge.tsx` — se aplicó un fix de bajo riesgo (tokens `--badge-*-bg` con alfa oklch literal), pero **el video recibido después la contradice como causa principal**. **Análisis del video** (`bug cel.mp4`, extraído frame a frame con ffmpeg): el glitch ocurre en los ~200ms siguientes a un scroll rápido — se ven varias filas superpuestas/fantasma a la vez ("46 de 46 clientes" repetido 5-6 veces, nombre de cliente y botón WhatsApp duplicados), la firma típica de que el navegador **no llega a repintar todas las filas a tiempo durante un fling rápido** (jank de scroll), agravado por el desenfoque de movimiento de la cámara que filmó la pantalla. Candidato real: `ClientsTable.tsx` renderiza **las 46 filas sin paginar/virtualizar** (= gap ya anotado en **WEB-8**), cada una con Badges/botones/sombras — en un celular de gama media eso puede trabarse visualmente en scroll rápido. El fix de Badge (arriba) no es incorrecto pero probablemente no ataca la causa real. **Aplicado**: paginación de `ClientsTable` (WEB-8). **Probado por el usuario en su celular real: el glitch sigue** (mandó una captura fija, no video — confirma que no es solo desenfoque de cámara, hay algo real en esa franja). La captura muestra la estática justo en la fila de buscador/filtros de `/clientes` en mobile. **Rediseño aplicado (2026-07-11)**: en mobile esa fila tenía demasiados elementos (buscador + botón Filtros + selects/combobox colapsables + 3 botones toggle Vencidos/Contactados/Sin asignar siempre visibles, todo compitiendo por el ancho) — a pedido del usuario, ahora en mobile **solo se ve el buscador + un botón ícono de filtros** (con contador de filtros activos); todo lo demás (selects, combobox, y los 3 botones toggle que antes estaban siempre visibles) se movió detrás de ese botón, en un panel colapsable en columna. En desktop (`sm:` en adelante) el comportamiento no cambió — todo sigue visible en la fila como antes. `tsc`/`build` OK. **Sin confirmar todavía si esto resuelve el glitch en el celular real** — no se pudo probar interactivo en el navegador de esta sesión (login real + el emulador de viewport mobile de esta sesión no forzó el ancho angosto) |
| WEB-18 | **Invitar colaborador con selector de rol** | ✅ hecho (2026-07-11) | Migración `0023_invite_with_role.sql`: `invite_member(p_email, p_role default 'seller')` agrega al setting correcto (`allowed_emails`/`superadmin_emails`/`viewer_emails` — nuevo, ligado a PERM-1) y promueve directo desde `pending`; `revoke_member`/`uninvite_member` generalizados a las 3 listas y a cualquier rol activo, con el mismo guard "al menos un admin" que `set_user_role`. `TeamManager.tsx`: título "Invitar a un colaborador" + selector Vendedor/Lector/Administrador, badge de rol en invitaciones pendientes, sección nueva "Lectores" (espejo de "Administradores") con "Bajar a vendedor". `tsc`/`build` OK. **Sin probar interactivamente en el navegador** (todas las acciones requieren sesión superadmin real) — pendiente de confirmación visual del usuario |
| WEB-27 | **Vista Kanban (tablero) de clientes** | ✅ hecho (2026-07-18) | Pedido de Alejo (superadmin en la web): ver el embudo como tablero y cambiar el estado "en el momento". Nueva vista "Tablero" que **convive con la Tabla** vía un toggle (`ClientsView.tsx`, recuerda la elección en localStorage). `ClientsBoard.tsx`: 4 columnas = estados (semáforo `bg-warning/orange/success/destructive` como Reportes), tarjetas = clientes, **drag & drop HTML5 nativo** (sin dependencias) → al soltar cambia el estado con update optimista + `supabase.update({status}).eq('id')` + `router.refresh()`, revierte si falla. Clic en tarjeta abre el `ClientDrawer` (comentar/etiquetar/editar). `viewer` = solo lectura (sin drag); RLS limita al `seller` a los suyos. Buscador simple. Reusa `ClientDrawer`, `isFollowUpOverdue`/`formatFollowUpLabel`, `STATUS_LABELS`. `tsc` limpio + `next build` verde. **Sin probar en navegador** (login Google). **Mejoras WEB-27b (2026-07-18)** tras revisión con el usuario: (1) **coherencia del flujo** — mover a "Contactado" abre `BoardMoveDialog` (canal/resultado/próximo seguimiento, con defaults llamada·contestó·en 3 días) que registra la interacción + agenda seguimiento, con escape "Solo cambiar estado"; (2) **táctil/teclado** — `<select>` de estado en cada tarjeta (mismo `moveTo` que el drag); (3) **red de seguridad** — "Deshacer" en el toast de movimientos simples + confirmación al pasar a Ganado/Perdido (con nota de cierre opcional); (4) **escala/filtros** — scroll + "cargar más" por columna (`PAGE_SIZE=15`) y filtro por vendedor (Combobox). `moveTo` centraliza los 3 flujos (registrar/confirmar/directo). `tsc` + `next build` verde. Sin probar en navegador |
| WEB-28 | **Rediseño minimalista de la página de Clientes** | ✅ hecho (2026-07-18) | El usuario la sintió sobrecargada (4 recuadros de stats + botones + toggle sutil + buscador + 6 filtros + contador) con info redundante (los mismos números en los recuadros, en los botones de filtro y ahora en el Inicio de UXR-5). Elegido: opción **Minimalista**. **Barra única** en `ClientsView`: buscador (elevado, compartido entre Tabla y Tablero) + **selector de vista claro** (Lista/Tablero con íconos `List`/`Kanban`, activo en `bg-primary` — resuelve "no entiendo cómo entrar al Kanban") + Nuevo/Importar (movidos desde `page.tsx`). `ClientesStats` **eliminado** (los stats viven en el Inicio, clicables — salda de paso el pendiente de UXR-5). `ClientsTable`/`ClientsBoard` reciben `search` por prop (sin buscador propio); el panel de filtros de la tabla queda **colapsado por defecto también en desktop** (antes `sm:flex` lo forzaba visible), detrás del botón `⚙ Filtros (N)`. `page.tsx` solo renderiza `AppShell`+`ClientsView`. UXR-5 (llegar con `?status=` filtrado) intacto. `tsc` limpio + `next build` verde. Sin probar en navegador |

### Unificar vistas admin/vendedor (eliminar `/vendedor`)
> Decisión 2026-07-10 (pedido del usuario + recomendación UX/CRM): el vendedor debe ver lo mismo que el admin, salvo lo que sea información sensible o una acción de gestión. Hallazgo clave: el RLS de `clients`/`interactions` (migración `0001`) **ya** limita a cada vendedor a `assigned_to = auth.uid()` — si usa el mismo componente que el admin, la consulta se recorta sola, no hace falta reescribir queries. Lo único que hay que ocultar por rol son controles de UI y accesos de nav. `v_seller_stats` (Reportes) sí compara vendedores entre sí → queda admin-only (estándar en CRMs: leaderboard es vista de manager). "Contactados" se fusiona como filtro dentro de Clientes en vez de página aparte (menos vistas, menos desalineación de datos). Conviene secuenciar junto con **PERM-1** (rol lector): mismo patrón de "ocultar según rol" en vez de un tercer árbol de vistas.

| ID | Tarea | Prioridad | Notas |
|---|---|---|---|
| WEB-19 | `AppShell` único con nav condicional por rol | ✅ hecho (2026-07-11) | `SidebarNav.tsx` filtra links por `profile.role` (Equipo/Reportes/Configuración admin-only, Contactos GHL admin+seller, Inicio/Clientes para todos incl. `viewer`). Se estandarizó en el patrón de `AppShell` (sidebar + drawer con hamburguesa) en vez del bottom-nav que tenía `SellerShell` — el admin tiene hasta 6 ítems de nav, un bottom-nav quedaría muy apretado. `requireSuperadmin()` ya no redirige `seller` a `/vendedor` |
| WEB-20 | Unificar `/` (Inicio) | ✅ hecho (2026-07-11) | `page.tsx` unificado con `requireMember()`: `seller` ve `ProgressBanner` + `SellerClients` (lo que vivía en `vendedor/page.tsx`), `superadmin`/`viewer` ven el dashboard agregado. Confirmado con `pg_get_viewdef` que `v_pending_clients` no tiene filtro `assigned_to` hardcodeado — depende 100% del RLS, sirve para ambos casos sin reescribir la query |
| WEB-21 | Unificar `/clientes` | ✅ hecho (2026-07-11) | La pieza de mayor riesgo del sprint: se le portó a `ClientDrawer` la lógica de negocio que solo tenía `SellerClientDrawer` (registrar interacción al contactar, comentario rápido, adjuntos) para no perder funcionalidad del vendedor al unificar. `ClientDrawer` ahora es *role-aware*: `viewer` sin controles de contactar/editar/asignar/borrar; `seller` edita datos/tags/notas/estado pero no reasigna ni borra (bulk actions y filtro "vendedor"/"sin asignar" ocultos en `ClientsTable`); `superadmin` todo. De paso suma `phone_2`/`email_2` (CONT-1) que el drawer de admin no tenía. `AddClientDialog` acepta `defaultAssignedTo` (el vendedor lo usa auto-asignado, reemplaza el form inline de `SellerClients`) |
| WEB-22 | Unificar `/contactos-ghl` | ✅ hecho (2026-07-11) | `GhlBrowser` ya soportaba `selfAssignId` — solo faltaba la page wrapper condicional por rol (`viewer` bloqueado, es una acción de importar/escribir) |
| WEB-23 | Fusionar "Contactados" como filtro en Clientes | ✅ hecho (2026-07-11) | `clientes/page.tsx` trae los `client_id` con interacción desde el lunes (misma query que tenía `vendedor/contactados/page.tsx`) y los pasa a `ClientsTable` como `contactedThisWeekIds`; toggle nuevo "Contactados esta semana" junto a "Vencidos", para todos los roles. No replica el listado interacción-por-interacción que tenía `ContactadosList`, es un filtro sobre la tabla de clientes (lo que pide el backlog) |
| WEB-24 | Reportes queda admin-only (confirmado, sin cambio técnico) | ✅ confirmado (2026-07-11) | `v_seller_stats` compara vendedores entre sí — se mantiene bloqueado por `requireSuperadmin()` tal cual está. Sin cambios de código |
| WEB-25 | Limpieza: borrar `web/src/app/vendedor/` y componentes sin uso | ✅ hecho (2026-07-11) | Borrados (verificados huérfanos con `grep` antes): `web/src/app/vendedor/` completo (3 rutas), `SellerShell.tsx`, `SellerNav.tsx`, `SellerClientDrawer.tsx`, `ContactadosList.tsx`. **Se conservan** `ProgressBanner.tsx` y `SellerClients.tsx` — siguen en uso desde la `/` unificada. De paso se encontró y corrigió una inconsistencia: `SellerClients.tsx` todavía abría el viejo `SellerClientDrawer` en vez del `ClientDrawer` unificado — corregido antes de borrar, para que el vendedor tenga la misma experiencia en toda la app. El borrado se frenó primero por el clasificador de auto-mode (acción irreversible sobre archivos preexistentes sin nombrarla explícitamente) — confirmado por el usuario antes de proceder |

### Permisos y seguimiento (rol lector, edición propia, adjuntos)
> Charla 2026-07-10: expansión de 3 ideas del usuario. Diseño acordado, falta bajar a migración.

| ID | Tarea | Prioridad | Notas |
|---|---|---|---|
| PERM-1 | Rol **lector** (viewer, global, app + web) | 🟠 backend hecho (2026-07-10) | Migración `0015_viewer_role.sql`: nuevo valor `viewer` en `profiles_role_check`, helper `private.is_read_all()` (superadmin o viewer) usado en las políticas SELECT de `profiles`/`clients`/`interactions`, `set_user_role` admite `'viewer'`. **Falta (Sprint 2)**: extender `invite_member` para invitar directo con rol (ver WEB-18) y ocultar en la UI (app y web) los botones de editar/contactar/asignar cuando `role='viewer'` — hoy solo está bloqueado por RLS, no oculto |
| PERM-2 | Edición de cliente en la app + **auditoría automática** | 🟠 backend hecho y probado (2026-07-11) | Migración `0018_client_changes_audit.sql`: tabla `client_changes` (cliente, quién, campo, valor viejo→nuevo, cuándo) poblada por trigger `AFTER UPDATE` en `clients`, RLS calcada de la ficha del cliente. Probado con transacción revertida: un `UPDATE` real generó la fila esperada; un `UPDATE` sin cambio de valor no generó ruido. **Falta (Sprint 2)**: la pantalla de editar cliente en la app (**APP-4**) y mostrar el historial de auditoría en la ficha (web y app) |
| PERM-3 | Adjuntar **fotos, PDFs y notas de voz** al seguimiento | ✅ hecho (2026-07-11) | Migración `0019`. Web: `SellerClientDrawer.tsx` — botón "Adjuntar" por ítem del historial (límite 10MB), listado con "Ver" vía URL firmada. App: se instalaron `expo-image-picker`, `expo-document-picker`, `expo-audio`, `expo-file-system` (`npx expo install`, versiones SDK 54) + plugins con textos de permiso en `app.json`. `ClientDetailScreen.tsx` — 3 botones por ítem del historial (Foto = galería vía `launchImageLibraryAsync`, PDF vía `expo-document-picker`, Nota de voz vía `useAudioRecorder` con toggle grabar/detener), sube con `File(uri).bytes()` (API nueva de `expo-file-system` en SDK 54, reemplaza `readAsStringAsync`+base64 — confirmado en la doc versionada, no es lo que yo hubiera asumido de memoria) directo a Storage como `Uint8Array` (tipo válido según `FileBody` de `@supabase/storage-js`, confirmado en las definiciones instaladas). Nuevas `uploadInteractionAttachment()`/`getInteractionAttachments()`/`getAttachmentSignedUrl()` en `api.ts`. `tsc` OK + `npx expo export --platform android` bundló limpio (1030 módulos, sin errores de resolución). **Sin probar en un dispositivo real** (solo bundling verificado, no comportamiento nativo real de cámara/mic/permisos). **Alcance acotado a propósito**: foto solo desde galería (sin cámara), sin comprimir imágenes antes de subir |
| PERM-5 | **Matriz de permisos editable en Configuración** | ✅ código listo (2026-08-15); `0032` aplicada (2026-08-15); ⏳ falta probar con sesión real | web-admin / backend-supabase | Pedido del usuario: llevar a Configuración el cuadro de qué rol ve qué sección, **editable**. Antes los permisos estaban escritos a mano en tres lugares sincronizados por convención (el array `LINKS` del sidebar, las guardas de cada página y dos `if (role === 'viewer') redirect` sueltos). **Ahora hay una sola definición**: `lib/sections.ts` (registro puro, sin React, que comparten servidor y cliente) + la key `role_permissions` en `app_settings`. **Guarda unificada**: `requireAccess(section)` en `lib/auth.ts` reemplaza a `requireMember`/`requireSuperadmin`, que **se borraron** — dejarlas habría garantizado que la próxima página use el atajo viejo y quede fuera de la matriz. Devuelve también las secciones permitidas, que `AppShell` recibe como **prop obligatorio**: si una página nueva se lo olvida, falla el build en vez de mostrar un menú de más. **Las 6 APIs migraron** a `apiSectionGuard` (`lib/api-auth.ts`), si no la matriz mentiría: habilitar Prospección al lector le daría la pantalla y 403 en cada llamada. `api/ghl/pipelines` se mapea a **Configuración** y no a Contactos GHL, aunque comparta carpeta. **Solo 4 de las 7 filas son editables** (Clientes, Prospección, Contactos GHL, Reportes): Inicio queda fija porque negarla deja al usuario rebotando al entrar, y Equipo y Configuración porque sus acciones están bloqueadas en la propia base — otorgarlas daría botones que fallan (D26). **Guardarraíles**: el superadmin no existe en el dato (auto-bloqueo imposible por construcción), trigger en la `0032` que descarta secciones y roles desconocidos, y el lector reimpone invariantes igual. **Fixes de paso**: `/no-autorizado` distinguía mal dos casos y le decía "esperando autorización" a un miembro válido que pegó una URL — ahora explica que la sección no está habilitada y ofrece volver al inicio; y `saveKey` usaba `.update()` sin verificar filas, así que decía "guardado" sin guardar cuando faltaba la fila (afectaba a las 10 tarjetas de Configuración). **Verificado**: `tsc` limpio, `next build` verde, y un chequeo automático de que el seed de la `0032` **replica exactamente** los defaults del código, así aplicarla no cambia nada. **Falta**: aplicar la migración y recorrer la pantalla con sesión real |

### Modernización UX/UI del panel (sprints)
> Recomendación 2026-07-10, a ejecutar en orden — cada sprint es entregable por separado.
> **Actualizado 2026-07-10**: se investigó [21st.dev](https://21st.dev/community/components) (librería de componentes React/Tailwind) como fuente de inspiración/base para cada sprint — ver columna "Fuente 21st.dev" y la nota técnica debajo de la tabla.

| ID | Tarea | Prioridad | Notas | Fuente 21st.dev |
|---|---|---|---|---|
| UX-1 | Fundamentos y consistencia | 🟡 | Auditoría de espaciado/tipografía/iconografía entre pantallas; estados vacíos/carga/error prolijos donde falten (hoy varios son genéricos) | Bajo valor acá: `ui/EmptyState.tsx` y `ui/Skeleton.tsx` propios ya son sólidos — no reemplazar, solo generalizar su uso donde falte. Si se quiere pulir más, categoría **Empty States** como referencia visual únicamente |
| UX-2 | Microinteracciones y transiciones | 🟡 | Animaciones en modales/drawers/toasts (Framer Motion), feedback en botones (loading/hover/press), transición entre páginas. Mayor impacto visual con menor riesgo | **Prerequisito**: `framer-motion` no está instalado en `web/package.json` todavía (`npm install framer-motion`). Categorías **Dialogs/Modals** (94) y **Toggles** (83) como referencia de patrones de animación a adaptar sobre `ClientDrawer.tsx`/`AddClientDialog.tsx` (los toasts ya animan bien vía `sonner`, no tocar) |
| UX-3 | Navegación y layout | 🟡 | Sidebar colapsable, breadcrumbs, búsqueda global Cmd/Ctrl+K (= WEB-10), responsive tablet | Categoría **Sidebars** (24) para el patrón colapsable — hoy `SidebarNav.tsx` es una lista fija sin collapse; conviene resolverlo junto con **WEB-19** (mismo archivo, nav condicional por rol, evita tocarlo dos veces). Categoría **Command Menu** (10, estilo cmdk) para Cmd/Ctrl+K — aditivo, bajo riesgo, no reemplaza nada existente |
| UX-4 | Tablas y dashboard | 🟡 | Rediseño de tablas (= WEB-8 paginación/virtualización), gráficos con mejor storytelling (= WEB-6 tendencia diaria), badges de vencidos/pendientes (= WEB-7) | Categoría **Tables** (76, varias con paginación/orden/filtro incorporados) como referencia de patrón para `ClientsTable.tsx` (hoy sin paginación, trae todo). Dashboards/**Charts** (30 bloques de dashboard) como referencia visual para `SellerChart.tsx` — la librería de charting sigue siendo `recharts`, no se reemplaza, solo el layout/storytelling. Para los badges de vencidos ver también SEM-1 más abajo (categoría **Badges**) |
| UX-5 | Accesibilidad y pulido final | 🟡 | Contraste, foco por teclado, revisión modo oscuro/claro pantalla por pantalla | No aplica una categoría de 21st.dev puntual — es auditoría manual sobre lo ya construido en los sprints anteriores |
| BRAND-1 | **Manual de identidad visual ST Labs + Turbo v0.1** | ✅ hecho (2026-08-14) | Manual y assets externalizados en el repositorio hermano `crm-hunter-leads-brand`; este proyecto conserva solo `docs/IDENTIDAD-VISUAL.md`. La implementación de UI queda como tarea futura y no se mezcla con este cambio. | — |
| BRAND-2 | **Aplicar la identidad ST Labs + Turbo a la UI (web + mobile)** | ✅ hecho (2026-08-14) | Contracara de BRAND-1. **Fuente**: <https://github.com/Sergio726/crm-hunter-leads-brand> (`identity-manual.html`), resumen en [`docs/IDENTIDAD-VISUAL.md`](IDENTIDAD-VISUAL.md). **(1) Color** — `globals.css` reescrito con la paleta del manual convertida a oklch: mint `#02FFC4` = `oklch(0.888 0.179 168.3)` como `--primary` en ambos temas, ink `#070908` de fondo oscuro, obsidiana `#0D1411` en cards, papel `#F4F7F5` en claro, y el trazo oscuro como mint-soft al 18% tal como lo define el manual. Se agregó `--primary-deep` (`#08785F`) porque **el mint no contrasta como texto sobre fondo claro**: lo usan `Badge` tone primary, el ícono de `StatCard` y `.eyebrow`. `--success` se corrió a hue 150 para no confundirse con el mint de acción (el semáforo SEM-1 se conserva). `--chart-1`/`--chart-3` se separaron por hue (168 vs 232) porque se superponen en el área de tendencia. Tokens crudos nuevos: `--brand-mint/-soft/-ink/-paper`. **(2) Tipografía** — `--font-mono` pasa a `Consolas, Geist Mono, …` (Consolas es de sistema; fuera de Windows cae en Geist Mono, que ya se cargaba). `h1/h2/h3` van en mono con interletrado negativo vía `@layer base`; `h4` queda en sans a propósito (es el nombre del cliente, texto de lectura). Utilidades nuevas: `.eyebrow` (rótulo `/ técnico`), `.metric` (cifras tabulares), `.brand-grid` (grilla fina), `.turbo-glow`. Aplicadas en `StatCard` (label + cifra), `SectionCard` h2, `AppShell` h1, `Badge`, y los `<thead>` de las **6 tablas**. **(3) Logo** — `Logo.tsx` reescrito: renderiza las variantes positiva y negativa y **el tema elige**, sin JS ni parpadeo; conserva el fallback de UXR-2. Los PNG traen fondo sólido, así que usan `mix-blend-multiply`/`screen` para no dejar un parche sobre la tarjeta (**verificado en el navegador**). **(4) Turbo** — nuevo `components/brand/TurboAvatar.tsx` (`TurboMark`/`TurboGlyph`/`TurboPortrait`); el chat de prospección ahora tiene cabecera con nombre y estado `/ listo`·`/ pensando`, avatar en cada mensaje del agente y retrato en el estado vacío; el sidebar usa su marca en vez del ícono `Radar`. Bautizado en el system prompt (`agent.ts`) con la personalidad del manual. Todo el copy que decía "el asistente" ahora dice Turbo. **(5) More Migraciones eliminado** — borrados `logo.png`, `LOGO MORE LIGHT.png` y `src/app/icon.png` (era la "M" de More, servía de favicon); nuevo `src/app/icon.svg` con el isotipo ST Labs reconstruido en vectores. Plantillas de email actualizadas (decían "Somos More" y cargaban el logo borrado). Los **dominios** `*.moremigracion.com` NO se tocaron: son infraestructura, no marca. **(6) Mobile** — `ui.ts` con la misma paleta + tokens nuevos `onPrimary` (el texto sobre mint es ink, nunca blanco) y `overlay`; `mono` por plataforma (Menlo/monospace); `Logo` usa el asset ST Labs; `ProgressBanner` rediseñado; `app.json` pasa a `userInterfaceStyle: automatic` con fondos ink. **Verificado**: `tsc` limpio en web y mobile, `next build` verde (22 rutas) y **login probado en Chrome en ambos temas**. **Sin probar**: las pantallas internas (requieren login, bloqueado por el OAuth del backend nuevo) y la app en dispositivo real | Referencia visual del propio manual, no de 21st.dev |

> **Nota técnica — cómo usar 21st.dev en este proyecto**: el panel usa **shadcn/ui + Base UI + Tailwind v4** con wrappers propios (`components/ui/*`), no shadcn "puro". Los componentes de 21st.dev sirven como **referencia de patrón/estructura para adaptar**, no para copiar y pegar tal cual — hay que ajustarlos a los tokens oklch y a los wrappers existentes (`Button`, `Card`, `Badge`, etc.) para no romper la consistencia visual ya lograda (rediseño premium 2026-07-08). El MCP `21st-dev-magic` (herramientas `21st_magic_component_builder`/`_inspiration`/`_refiner`) está conectado en esta sesión pero **devolvió error al usarse** — la causa más probable es que falta o es inválida la `API_KEY` (se obtiene gratis en `21st.dev/magic/console` y se configura en `~/.claude.json` → `mcpServers.21st-dev-magic.env.API_KEY`). Sin eso, hay que navegar el sitio a mano para copiar código de un componente puntual.
>
> **Decisión 2026-07-10**: el usuario evaluó 21st.dev vs. shadcn/ui y confirmó **shadcn/ui** como fuente preferida (`web/components.json` ya tiene la CLI configurada — `style: "base-nova"`, Base UI, mismo sistema de diseño del panel, cero fricción de adaptación). 21st.dev queda como inspiración puntual secundaria, no como fuente principal.

### 10 recomendaciones concretas de UX/UI (dashboard, reportes, login, tarjetas)
> Investigado 2026-07-10 con 3 agentes de exploración sobre el código real (login/shell, dashboard/tarjetas, reportes/tabla) a pedido del usuario. Cruza con `UX-1`…`UX-5`, `WEB-6`, `WEB-7`, `WEB-19`, `SEM-1` donde corresponde — no los duplica. Deja afuera a propósito la paginación de `ClientsTable` (= WEB-8) y la animación del `ClientDrawer` (= UX-2) para no repetir lo ya listado.

| ID | Tarea | Prioridad | Notas |
|---|---|---|---|
| UXR-1 | Mostrar el error de login OAuth en vez de fallar en silencio | ✅ hecho (2026-07-10) | `login/page.tsx` ahora lee `?error=auth` (envuelto en `Suspense` por `useSearchParams`) y muestra `toast.error`, limpiando la URL con `router.replace`. `tsc`/`lint`/`build` OK |
| UXR-2 | Bug: tokens de color rotos en el logo de respaldo | ✅ hecho (2026-07-11, probado en navegador) | `components/brand/Logo.tsx`: `text-primary-fg`→`text-primary-foreground`, `text-text`→`text-foreground`. **Bug nuevo encontrado al probar**: el `<img>` viene en el HTML de SSR, así que si `/brand/logo.png` falla rápido (ej. 404), el navegador dispara el evento `error` **antes** de que React hidrate y conecte `onError` — el fallback nunca se activaba. Fix: chequeo adicional en `useEffect` (`img.complete && naturalWidth===0` al montar) además del `onError`. Verificado en Chrome: forcé el 404 renombrando el archivo, confirmé el wordmark con colores reales (`getComputedStyle`), restauré el archivo y confirmé que el camino normal sigue andando |
| UXR-3 | Reemplazar el `UserMenu` "casero" por un `DropdownMenu` real | 🟡 | Hoy es `useState` + `fixed inset-0` manual en vez de un primitivo de Base UI/shadcn — pierde focus trap, navegación por teclado y ARIA que vendrían gratis |
| UXR-4 | Sidebar colapsable + breadcrumbs en el header | 🟡 | `SidebarNav.tsx` es una lista fija de 240px sin modo compacto; `AppShell.tsx` solo pinta `<h1>{title}</h1>` sin jerarquía de ruta. Mismo alcance que **UX-3**; conviene resolverlo junto con **WEB-19** (mismo archivo, nav condicional por rol) |
| UXR-5 | Unificar y potenciar las stat cards (fusionar `StatCard` + lógica ad-hoc de `ClientesStats`) | 🟡 parcial (2026-07-18) | **Hecho: tarjetas del Inicio clicables** — `StatCard` acepta `href` opcional (se renderiza como `Link` con hover elevado); en el dashboard "Clientes totales/Pendientes/Vencidos/Contactados/Ganados" linkean a `/clientes` filtrado (`?status=…` / `?overdue=1`). `clientes/page.tsx` lee `searchParams` (Promise, Next 16) y pasa `initialStatus`/`initialOverdue` a `ClientsTable` (valores iniciales de sus filtros); `ClientsView` fuerza la vista Tabla cuando se llega con filtro. `tsc`+`next build` verde. **Pendiente**: fusionar `StatCard` con la lógica ad-hoc de `ClientesStats` (siguen siendo dos implementaciones) |
| UXR-6 | Dashboard: sumar tendencia temporal + feed de actividad reciente | ✅ hecho (2026-07-17) | = **WEB-6** (implementado junto). Agregada la comparación vs. período anterior (deltas semanales en StatCards) y el feed de últimos agregados/contactados/ganados, además del gráfico de tendencia diaria. Reusó el patrón de `recharts`/tokens de `SellerChart.tsx` |
| UXR-7 | Badges de pendientes/vencidos visibles en dashboard y sidebar | ✅ hecho (2026-07-18) | = **WEB-7** (implementado junto). Sidebar: badges de pendientes/vencidos. Dashboard: `StatCard` extendido con prop `tone` ('default'/'warning'/'danger', colorea el recuadro del ícono con los tokens de `Badge`); se agregó una card **"Vencidos"** (tono danger, ícono `AlertTriangle`) y "Pendientes" toma tono warning cuando hay >0. Grid del dashboard pasó a 6 columnas. Reusó `isFollowUpOverdue` de `format-dates` para contar vencidos |
| UXR-8 | Conectar `Skeleton.tsx` a loading states reales | 🟡 | El primitivo existe pero **no tiene ningún consumidor** en `web/src/`; no hay `loading.tsx` en ninguna ruta. Los estados de carga actuales son solo texto plano ("Redirigiendo…", "Entrando…", "Cargando…" en login, `auth/confirm` e historial de `ClientDrawer`) |
| UXR-9 | Reportes: funnel interactivo + selector de período + indicar el truncado de vendedores | 🟡 | El embudo de conversión son `<div>` con `width: %` a mano (no recharts); `SellerChart` trunca a `.slice(0, 8)` vendedores sin avisar que hay más; no hay comparación semana vs. semana anterior ni selector de rango |
| UXR-10 | Feedback visual en `ExportButton` + ampliar qué exporta | 🟡 | La descarga de CSV no dispara ningún `toast` ni loading (inconsistente con el resto de la app, que usa `sonner` en casi todo); solo exporta la tabla de vendedores, no el funnel ni los totales |

### Semáforo de estado de seguimiento (colores)
> Idea del usuario 2026-07-10 + hallazgo: app y web hoy usan colores **distintos** para el mismo estado `lost` (rojo en la app vs gris en la web) — no es solo una idea nueva, corrige una inconsistencia real.

| ID | Tarea | Prioridad | Notas |
|---|---|---|---|
| SEM-1 | Unificar semáforo de estado en app + web | ✅ hecho (2026-07-12) | 🟢 `won` verde (sin cambio) · 🟠 `pending` ámbar / `contacted` **naranja** (dos tonos cálidos distinguibles; antes `contacted` era azul en ambos) · 🔴 `lost` **rojo** (antes gris en la web/reportes). Se agregó un tono `orange` nuevo: web `globals.css` (token `--orange` + `--badge-orange-bg` en claro/oscuro, registrado en `@theme`) + `Badge.tsx`; mobile `ui.ts` (paleta claro/oscuro). `STATUS_TONE` unificado en `web/src/lib/types.ts` (antes duplicado en `ClientsTable.tsx` y `ClientDrawer.tsx`, ahora ambos lo importan); `reportes/page.tsx` `FUNNEL_COLORS` alineado; mobile `ClientCard.tsx` `statusColor` alineado. **Verificado en el navegador** (claro y oscuro): los 4 estados se leen bien y ámbar≠naranja. `tsc` OK web+mobile, `build` web OK. Mobile solo `tsc` (sin emulador) |
| SEM-2 | Que "vencido" no se muestre en clientes `lost` | ✅ hecho (2026-07-12) | Web ya lo cumplía (`isFollowUpOverdue` en `lib/format-dates.ts` excluye `won` y `lost`). **Faltaba en mobile**: `ClientCard.tsx` marcaba "Seguimiento vencido" para cualquier `next_follow_up` pasado, incluso `won`/`lost` — corregido para excluir ambos, igual que la web. `tsc` OK |

### Comentario rápido en el seguimiento
> Idea del usuario 2026-07-10.

| ID | Tarea | Prioridad | Notas |
|---|---|---|---|
| NOTE-1 | Comentario rápido (nota libre) en el seguimiento | ✅ hecho (2026-07-11) | Migración `0020_note_channel.sql` + fix `0022_fix_note_outcome_check.sql` (bug real encontrado y corregido probando: el constraint original solo bloqueaba en una dirección). UI: botón "Comentario rápido" con textarea inline en `SellerClientDrawer.tsx` (web, ficha del vendedor) y en `ClientDetailScreen.tsx` (app) — inserta `interactions` con `channel='note'`, sin `outcome`, **sin** cambiar `status`/`next_follow_up` del cliente (a diferencia de `logInteraction`, que sí lo hace). Nueva función `addQuickNote()` en `mobile/src/lib/api.ts`. Insert exacto probado contra Supabase real (transacción revertida). `tsc`/`build` OK. **No se agregó** en el `ClientDrawer.tsx` de admin (esa vista no tiene ningún flujo de registrar interacción hoy, es un gap preexistente separado) ni como acción inline en las tarjetas de lista (se puso en la ficha/detalle, que es donde ya vive el resto del registro de contactos) |

### Editar mi perfil
> Idea del usuario 2026-07-10. Chequeado: no hay riesgo de seguridad al sumar campos — `profiles` ya usa permiso a nivel de columna (`grant update (full_name, avatar_url) ... to authenticated`, migración `0001` línea 91), así que un vendedor nunca pudo auto-promoverse el `role` por acá.

| ID | Tarea | Prioridad | Notas |
|---|---|---|---|
| PROF-1 | Pantalla "Mi perfil" (app + web) | ✅ hecho (2026-07-11) | Migración `0016` (columnas) + `0024_avatars_bucket.sql` (bucket público de lectura, solo el dueño sube/reemplaza/borra la suya vía `storage.foldername(name)[1] = auth.uid()`, probado con `select` directo). Web: página `/mi-perfil` (`ProfileForm.tsx`, link nuevo en `UserMenu.tsx`) con nombre/teléfono/email secundario + subida de foto (file input, upload a Storage, cache-bust por querystring). App: mismos 3 campos en `ProfileScreen.tsx` (tab "Perfil" ya existente), nueva `updateMyProfile()` en `api.ts`. **Sin subida de foto en la app** (requeriría `expo-image-picker`, no instalado — se dejó afuera para no sumar una dependencia nativa sin poder probarla en dispositivo). `tsc`/`build` OK en ambos. **Sin probar interactivamente** (requiere sesión real) |

### Notificaciones al vendedor por email/WhatsApp/SMS
> Idea del usuario 2026-07-10, usando los servicios de envío de GHL vía n8n, preparado para otros CRMs a futuro (mismo principio multi-CRM de `ARCHITECTURE.md`).

| ID | Tarea | Prioridad | Notas |
|---|---|---|---|
| NOTIF-1 | Workflow n8n "CRM Lite — Notify User" | 🟡 backend hecho + workflows escritos (2026-07-12) | **Backend (migración `0025_notify_user.sql`, aplicada a cloud y verificada e2e)**: dos eventos con payload normalizado `{ event, user:{id,email,phone,name}, preferred_channel, client, message }`. (1) `lead.assigned` — trigger `clients_notify_lead_assigned` (pg_net) al asignar un cliente a un vendedor (`role='seller'`, evita ruido de imports GHL al superadmin). (2) `followup.overdue` — RPC `n8n_list_overdue_followups` que consulta el cron. Tabla `notifications` para dedupe (1 vencido por cliente/día vía índice único) + auditoría; RPC `n8n_mark_notified`. **SAFE-BY-DEFAULT**: sin `app_settings.n8n_notify_url` el trigger no dispara nada. Probado con transacción revertida (trigger genera 1 fila), RPC devuelve payload correcto con dato real, rechaza secreto inválido, advisors limpios. **Workflows n8n escritos y versionados** (`ghl/notify-user.json` webhook + `ghl/notify-overdue.json` cron diario), sumados a `deploy-workflows.ps1`. **FALTA (necesita el usuario)**: (a) desplegar los workflows (`deploy-workflows.ps1`, requiere `apikeyn8n`), (b) setear `n8n_notify_url`, (c) **verificar/testear la mensajería GHL** — la Conversations API (`/conversations/messages`, versión de header + forma del body) y el upsert del vendedor como contacto interno no se pudieron probar sin credenciales GHL. Ver `docs/INTEGRACION-N8N.md`. Nota: **podría destrabar WA-1** (usar el número ya aprobado de GHL). Contrato normalizado documentado. **2026-07-17 — workflows desplegados por el usuario y activos**. Apareció un error real en Discord: `429 The service is receiving too many requests` en el nodo `GHL Send Message`. Causa descartada: NO es loop (el trigger `lead.assigned` es `update of assigned_to` + guard, el write-back de GHL no lo re-dispara). Causa real: ráfaga de asignaciones dispara varios webhooks casi simultáneos y cada uno hace 2 llamadas seguidas a GHL (upsert vendedor + envío) → supera el burst de la API; `notify-user` no tenía ningún reintento. **Fix aplicado (versionado, rama `worktree-notif-1-rate-limit-retry`)**: `retryOnFail`/`maxTries:5`/`waitBetweenTries:5000` en los 2 nodos GHL de `notify-user.json` (un 429 espera hasta 5s y reintenta, ventana total ~20s > ventana de 10s de GHL); `notify-overdue.json` suavizado (batch 5→3, Wait 0.3→1.5s). El usuario activó el toggle "Retry On Fail" a mano en los 2 nodos GHL (verificado por API: `retryOnFail=true`, con los defaults de n8n 3 intentos/1s — no los 5/5s versionados; suficiente para el caso típico, conviene subirlo en un redeploy). **Verificación GHL e2e (2026-07-17)**: con las credenciales que dejó el usuario en `mobile/.env` (`GHL_API_KEY` PIT + `GHL_LOCATION_ID`), token válido con scopes contacts/locations/conversations (200); **envío probado replicando el body exacto de n8n**: `POST /contacts/upsert` → 201 y `POST /conversations/messages` (type Email) → 201 `"Email queued successfully"`. **Queda confirmada la forma del body de la Conversations API** (era el pendiente histórico). Contacto de prueba creado en GHL: `CRM Lite Test` (id `OPfnad4zhuXYaH65qAdj`, tag `crm-lite:internal-user`) — se puede borrar. **Fix de paso**: `notify-overdue.json` versionado apuntaba a la instancia vieja `stlabs.ar`; corregido a `moremigracion.com` (producción ya estaba bien, pero un redeploy la habría roto). **Higiene pendiente**: el `GHL_API_KEY` quedó en `mobile/.env` (solo debería tener URL pública + publishable key) — ya está también en `crm-secrets.local.env`, conviene borrarlo de `mobile/.env` |

### Segundo contacto del cliente
> Idea del usuario 2026-07-10.

| ID | Tarea | Prioridad | Notas |
|---|---|---|---|
| CONT-1 | Segundo teléfono/email en `clients` | ✅ hecho completo (2026-07-17) — sync GHL descartado por decisión del usuario (D11) | Migración `0017_client_second_contact.sql`: columnas `phone_2`/`email_2` en `clients`. **UI completa** en alta y edición, web y app: edición ya estaba (web `ClientDrawer.tsx`, mobile `ClientDetailScreen.tsx`); **alta agregada 2026-07-12** (web `AddClientDialog.tsx` con inputs "Teléfono/Email secundario" + insert; mobile `AddClientScreen.tsx` + campo `phone_2`/`email_2` en la interface `NewClient` de `api.ts`). `tsc` OK web+mobile, `build` web OK, modal de alta verificado en el navegador. Mobile solo `tsc` (sin emulador). **Sigue pendiente (aparte, no bloquea la UI)**: sync a GHL — verificar si la API v2 de GHL soporta un 2º email/teléfono (candidato: `additionalEmails`) antes de mapearlo al contrato normalizado; hoy `phone_2`/`email_2` no se envían al CRM. **Avance 2026-07-17**: mapeado el flujo completo del push — (1) `push_to_crm()` (migración `0011`) arma el jsonb `contact` con `{id,full_name,phone,email,company,status,tags}`; (2) `mark_crm_dirty()` marca dirty solo si cambian esos campos; (3) `n8n_crm_list_pending()` (retry) devuelve el mismo set; (4) el workflow `push.json` nodo "Map to GHL" arma `ghlBody={locationId,name,email,phone,companyName,tags}` → `POST /contacts/upsert`. Para CONT-1 hay que tocar los 4 lugares + agregar el mapeo secundario. **Bloqueado**: la verificación empírica del soporte GHL (`additionalEmails`/`additionalPhones`) quedó a medias — el token PIT (`GHL_API_KEY` de `mobile/.env`) se invalidó a mitad de las pruebas (`401 "token not valid"` en todos los endpoints, tras ~6 llamadas OK). Falta que el usuario regenere/valide el token para confirmar qué soporta GHL antes de escribir la migración. Hipótesis de doc (sin confirmar): `additionalEmails` (array) es estándar en GHL v2; `additionalPhones` probablemente NO nativo (el modelo tiene un solo `phone`) → phone_2 iría por custom field o queda fuera del alcance inicial. **CERRADO 2026-07-17 (D11)**: el usuario decidió que el 2º contacto NO se sincroniza a GHL — vive solo en CRM Lite. No se implementa el sync; CONT-1 queda completo con la UI ya hecha |

### Plan de sprints (orden de ejecución sugerido)
> Para que otra sesión, otro desarrollador u otro agente sepa por dónde arrancar sin releer toda la conversación del 2026-07-10. Cada sprint es un entregable separado; los agentes sugeridos son los de `.claude/agents/`.

| Sprint | Contenido | Agente(s) | Depende de |
|---|---|---|---|
| 0 — Urgente | ✅ hecho — Fix retry n8n · WEB-17 (UI convertir vendedor en admin) | `integrations-n8n`, `web-admin` | — |
| 1 — Migraciones base | ✅ backend hecho (2026-07-10) — PERM-1 (rol lector) · PROF-1 (columnas de perfil) · CONT-1 (`phone_2`/`email_2`) · PERM-2 (auditoría) · PERM-3 (adjuntos) · NOTE-1 (canal `'note'`). Migraciones `0015`→`0021`, `get_advisors` limpio | `backend-supabase` (una sola sesión, tocan las mismas tablas) | Sprint 0 |
| 2 — Pantallas | ✅ hecho (2026-07-11) — Mi perfil · APP-4 (editar cliente) · comentario rápido · WEB-18 (invitar con rol) · adjuntos en ficha (foto/PDF/nota de voz), completos en app y web. **Sin probar en dispositivo real** (solo `tsc`/`build`/bundling) | `mobile-app` y `web-admin` en paralelo | Sprint 1 |
| 3 — Unificar vistas | ✅ hecho (2026-07-11) — WEB-19→25 (elimina `/vendedor`, nav condicional por rol). `tsc`/`build` OK, sin probar en sesión real todavía | `web-admin` | Sprint 1 (para diseñar el nav ya con el rol lector) |
| 4 — Notificaciones | NOTIF-1 | `integrations-n8n` + `backend-supabase` | Sprint 1/2 (teléfono del vendedor) |
| 5 — Semáforo | ✅ hecho (2026-07-12) — SEM-1/2. Tono `orange` nuevo (web+mobile), `contacted` naranja, `lost` rojo en todos lados, `STATUS_TONE` unificado en `lib/types.ts`, "vencido" no aplica a won/lost. Verificado en navegador (claro/oscuro) | `mobile-app` + `web-admin` | — (sin dependencias, cualquier hueco libre) |
| 6 — Modernización | UX-1→5 | `web-admin` | Idealmente al final |

### n8n / integración GHL
| ID | Tarea | Prioridad | Notas |
|---|---|---|---|
| N8N-0 | Organizar workflows en carpeta CRM Lite (repo + panel) | ✅ hecho | Repo: `n8n/workflows/crm-lite/`; panel manual |
| N8N-4 | Proteger webhooks (header secreto) | ✅ hecho | Credencial `rZvKjdRnF39vlXHi`; 403 sin header |
| N8N-5 | Write-back `crm_contact_id` | ✅ hecho | Migración `0011` corregida y aplicada; **probado e2e** (alta + edición, sin loop) 2026-07-09 |
| N8N-6 | Reintentos cron | ✅ hecho | `retry.json` activo (corregido: secreto vía Header Auth) |
| N8N-7 | Inbound GHL → Supabase | ✅ hecho | Webhook registrado en GHL y **probado e2e** (alta+edición+tags+empresa) 2026-07-09. El flujo re-consulta la API de GHL: el payload solo necesita `id` |
| N8N-8 | Alertas Discord | ✅ hecho | `shared/alerts.json` + errorWorkflow |
| N8N-9 | Auto-import por tag | ✅ hecho | `auto-import.json` + UI Configuración |
| N8N-10 | Mapeo status → stages | ✅ hecho (v1) | `pipelines.json` + JSON en Configuración |
| N8N-11 | Tags bidireccionales | ✅ hecho | Convención `crm-lite:` en push + inbound |
| N8N-12 | Plantillas HubSpot/Pipedrive | ✅ hecho | `hubspot/`, `pipedrive/` inactivas |
| N8N-16 | **Interruptor pausar sync GHL** (`crm_sync_enabled`) | ✅ hecho código (2026-08-13); ⏳ aplicar migración en cloud | web-admin + backend-supabase | UI en Configuración + migración `0027`. Pausa push/inbound/auto-import/retry/notify sin vaciar URLs (D12). MCP sin acceso a `CRM.LITE` en esta sesión — falta correr el SQL en el SQL Editor / CLI con acceso al proyecto |

### App móvil
| ID | Tarea | Prioridad | Notas |
|---|---|---|---|
| APP-1 | Build EAS + publicar (= PROD-1): ícono, deep link `crmlite://` prod | 🔴 | instalable sin Metro |
| APP-2 | Notificaciones push (recordatorios) — ver IDEA-3 | 🟠 | Expo push |
| APP-3 | WhatsApp API real (Evolution API del VPS) — ver WA-1 | 🟠 | |
| APP-4 | Editar cliente desde la app | ✅ hecho (2026-07-11) | Botón lápiz junto al nombre en `ClientDetailScreen.tsx` abre un modal (mismo patrón que el de "¿Cómo resultó?") con nombre/teléfono/email/teléfono secundario/email secundario/empresa/tags/notas, usa `updateClient()` ya existente en `api.ts`. A propósito **no** incluye estado ni vendedor asignado (eso se maneja vía el flujo de contactar/resultado y es admin-only respectivamente). Update de `phone_2`/`email_2`/`tags` probado contra Supabase real (transacción revertida). `tsc` OK |
| APP-5 | Modo offline (encolar interacciones y sincronizar) | 🟠 | |
| APP-6 | Buscar / ver todos los clientes (no solo pendientes) | ✅ hecho (2026-07-17) | Nuevo tab **"Clientes"** (ícono lupa) entre Pendientes y Contactados: `ClientsScreen.tsx` con buscador fijo arriba (filtra en memoria por nombre/empresa/teléfono/email/`phone_2`/`email_2`/tags, ignorando acentos) + lista de todos los clientes del vendedor vía `getAllClients()` (nueva en `api.ts`, tabla `clients`, el RLS ya recorta a `assigned_to`). Reusa `ClientCard` y el FAB de alta; contador "X de Y clientes"; pull-to-refresh. `Clientes` sumado a `TabsParamList` + `App.tsx`. `tsc` limpio y `expo export` (android) bundleó OK. **Sin probar en dispositivo real** (solo bundling). Para el superadmin el tab trae todos por RLS, pero está pensado para el vendedor (el admin gestiona desde la web) | |
| APP-7 | Filtrar por tag / ver tags | 🟡 | |
| APP-8 | Gamificación (ranking del equipo, logros, historial de rachas) | 🟡 | |
| APP-9 | Adjuntar foto / nota de voz a una interacción | 🟡 | |
| APP-10 | Biometría / PIN para abrir la app | 🟡 | |
| APP-11 | **Cambiar estado (Ganado/Perdido) desde la app** | ✅ hecho (2026-07-18) | Gap detectado al revisar si la app quedó acorde a los cambios web: el vendedor solo podía llegar a "Contactado" (vía el flujo de contacto); no había forma de **cerrar** un cliente (Ganado/Perdido) desde el celular, aunque la web ya lo permite (Kanban/ficha). `ClientDetailScreen.tsx`: selector de estado con **chips tocables** (Pendiente/Contactado/Ganado/Perdido) con el color del semáforo (SEM-1), el actual resaltado; tocar otro llama `updateClient({status})` + recarga. **Confirmación** (Alert nativo) al pasar a Ganado/Perdido, igual que la red de seguridad de la web (WEB-27b). El RLS limita al vendedor a sus propios clientes. `tsc` limpio + `expo export`. Sin probar en dispositivo real |

### Transversal
| ID | Tarea | Prioridad | Notas |
|---|---|---|---|
| TRV-0 | **Migración servidor nuevo** (web Docker + n8n) | 🔴 en curso | Preparado 2026-07-09: repo `somosmore/CRM-Lite`, `web/Dockerfile`, script n8n parametrizado, guía `MIGRACION-SERVIDOR.md`. Bloqueado por: instalar Dokploy + DNS (usuario) |
| TRV-1 | Seguridad: SEC-3 (JWT respaldo) + revisar advisors + proteger webhooks | 🟠 | |
| TRV-2 | CI: correr `tsc`/`lint`/`build` en cada push | 🟡 | |
| TRV-3 | Backups verificados de la base | 🟡 | |

---

## ✅ Hecho (log)

| Fecha | Tarea |
|---|---|
| 2026-08-15 | **La app quedó desplegada y en uso real** (PRs #3 a #10). Deploy en Vercel (`WEB-29`) con Root Directory `web`; identidad aplicada y producto renombrado a **Hunter Leads, potenciado por Turbo** (D23); Google OAuth habilitado y primer login real como superadmin; Prospección con arreglo de móvil, flujo numerado, **20 países** y **15 rubros** (`PROSP-7`); rebalanceo del verde (D24); y `PROSP-6` Fase 0 validada con datos reales. **Tres bugs preexistentes destrabados**: el enlace por email nunca entraba (leía el formato viejo, Supabase manda PKCE); **el chat de Turbo nunca funcionó** —una raya larga en un header hacía fallar toda llamada a OpenRouter, D25—; y el sitio daba 404 porque Vercel apuntaba a la raíz del repo. Ver punto **0h** de `STATE.md` para el detalle, y sobre todo para **qué se intentó y no funcionó** |
| 2026-08-14 | **BRAND-2 — Identidad ST Labs + Turbo aplicada a la UI**: paleta del manual en oklch (mint `#02FFC4` como primary, ink/obsidiana/papel), Consolas como voz técnica en títulos/cifras/rótulos/tablas, logotipo ST Labs con variante por tema, y **Turbo con cara y nombre** en el chat de prospección y el sidebar. Eliminado todo el branding de More Migraciones (logos, favicon "M", plantillas de email); los dominios `*.moremigracion.com` se conservan por ser infraestructura. Mobile: misma paleta + token `onPrimary`, `ProgressBanner` rediseñado, `userInterfaceStyle: automatic`. Dos decisiones de contraste que el manual no cubría: `--primary-deep` para texto verde sobre fondo claro, y `--success` corrido a hue 150 para no chocar con el mint (D20/D21). `tsc` web+mobile limpio, `next build` verde, login verificado en Chrome en claro y oscuro |
| 2026-08-13 | **N8N-16 — Interruptor sync GHL**: setting `crm_sync_enabled` + guards en push/notify/pending/inbound/settings (migración `0027`); UI en Configuración. Código listo; falta aplicar SQL en cloud (MCP sin acceso a CRM.LITE). D12 |
| 2026-07-18 | **APP-11 — Cambiar estado desde la app**: al revisar si la app quedó acorde a los cambios web, se detectó que el vendedor no podía marcar Ganado/Perdido desde el celular (solo llegaba a Contactado). `ClientDetailScreen.tsx`: chips de estado tocables con el semáforo + confirmación al cerrar. `tsc`+`expo export`. Sin probar en dispositivo |
| 2026-07-18 | **WEB-28 — Rediseño minimalista de Clientes**: barra única en `ClientsView` (buscador compartido + selector de vista claro Lista/Tablero + Nuevo/Importar); `ClientesStats` eliminado (los stats están en el Inicio, clicables); filtros colapsados por defecto también en desktop. `ClientsTable`/`ClientsBoard` reciben `search` por prop. `tsc`+`next build` verde |
| 2026-07-18 | **UXR-5 (parcial) — Tarjetas del Inicio clicables**: `StatCard` con `href` opcional (Link); las cards de estado del dashboard linkean a `/clientes` filtrado (`?status=…`/`?overdue=1`). `clientes/page.tsx` lee `searchParams` y pasa `initialStatus`/`initialOverdue` a la tabla; `ClientsView` fuerza vista Tabla si hay filtro. `tsc`+`next build` verde. Falta: fusionar `StatCard`+`ClientesStats` |
| 2026-07-18 | **WEB-27b — Mejoras del Kanban** (tras revisión): (1) mover a "Contactado" registra la interacción + agenda seguimiento vía `BoardMoveDialog` (defaults rápidos + escape "solo estado"); (2) `<select>` de estado por tarjeta para táctil/teclado; (3) "Deshacer" en toast + confirmación al cerrar Ganado/Perdido; (4) scroll + "cargar más" por columna y filtro por vendedor. `moveTo` centraliza los flujos. `tsc` + `next build` verde |
| 2026-07-18 | **WEB-27 — Vista Kanban de clientes**: nueva vista "Tablero" que convive con la Tabla en `/clientes` (toggle `ClientsView`, recuerda la elección). `ClientsBoard`: columnas por estado (semáforo), tarjetas de clientes, **drag & drop nativo** para cambiar el estado (update optimista + supabase + revertir si falla), clic abre la ficha (`ClientDrawer`). `viewer` solo lectura. Surgió del pedido de Alejo (embudo accionable) y un mockup aprobado. `tsc` + `next build` verde. Sin probar en navegador |
| 2026-07-18 | **WEB-7/UXR-7 — Badges de vencidos/pendientes**: sidebar con `Badge` de pendientes (Inicio, warning) y vencidos (Clientes, danger) —`AppShell` trae los contadores con el client supabase (RLS por rol) y los pasa al `SidebarNav`. Dashboard: `StatCard` con prop `tone`, card "Vencidos" (danger) + "Pendientes" warning cuando hay >0, grid a 6 columnas. `tsc` limpio + `next build` verde. Sin probar en navegador (login Google) |
| 2026-07-17 | **WEB-6/UXR-6 — Dashboard con tendencia + actividad**: Inicio admin/viewer suma fila "Esta semana" (StatCards Nuevos/Contactos con delta vs. semana anterior), `TrendChart` (recharts AreaChart, nuevos+contactos por día, 30 d) y `ActivityFeed` (agregados/contactados/ganados, fecha relativa). Agregación en JS (sin migración). `StatCard` con `delta` opcional; `formatRelativeTime()` nuevo. `tsc` limpio. Sin probar en navegador (login Google) |
| 2026-07-17 | **APP-6 — Buscar/ver todos los clientes (app)**: nuevo tab "Clientes" (lupa) con buscador que filtra en memoria (nombre/empresa/teléfonos/emails/tags, sin acentos) sobre todos los clientes del vendedor (`getAllClients()`, RLS recorta solo). Reusa `ClientCard`+FAB. `tsc` limpio + `expo export` android OK. Sin probar en dispositivo real |
| 2026-07-17 | **NOTIF-1 verificado + fix 429**: workflows en producción (`moremigracion.com`), corregido el `429 too many requests` de GHL con reintento en los 2 nodos GHL de `notify-user` + throttle más suave en `notify-overdue`; envío end-to-end probado contra GHL real (upsert 201 + `conversations/messages` 201). Corregida la URL vieja (`stlabs.ar`→`moremigracion.com`) en `notify-overdue.json` versionado. PR #11 |
| 2026-07-17 | **CONT-1 cerrado**: decisión de producto (D11) — el 2º teléfono/email (`phone_2`/`email_2`) NO se sincroniza a GHL, vive solo en CRM Lite. La UI (alta + edición, app y web) ya estaba hecha; se descarta el sync GHL que quedaba pendiente. Sin código nuevo |
| 2026-07-13 | **NOTIF-1 activado en producción + ajustes en vivo**: desplegados los workflows `Notify User`/`Notify Overdue` a `n8n.moremigracion.com` (webhook `crm-notify` pasó de 404→403), y prendido `app_settings.n8n_notify_url`. **Migración `0026_notify_include_superadmins.sql`**: el trigger `notify_lead_assigned` y la RPC de vencidos ahora notifican a **seller Y superadmin** (antes solo seller; el equipo real es mayormente superadmin, así que casi nunca disparaba), conservando el guard anti-spam (no notifica en el INSERT de import de GHL, `origin='ghl'`). Probado en transacción revertida (asignar a superadmin genera 1 notificación). **Resuelto de paso un duplicado tipo N8N-15**: "Johanna Diaz" cargada en app ya existía en GHL → el retry chocaba contra `clients_crm_contact_id_key` cada 15 min (alertas Discord). Fusionada en la fila GHL (nota preservada, asignada a soporte@justmore.net) y borrada la duplicada; sucios restantes = 0. **Pendiente**: confirmar que el mail sale de verdad (depende del scope `conversations/message.write` del token de GHL) — se ve en la ejecución de Notify User en n8n al asignar un lead a un superadmin/seller |
| 2026-07-12 | **Sprint 5 — Semáforo (SEM-1/2)**: unificado el color de estado entre app y web. Tono `orange` nuevo (web `globals.css`+`Badge.tsx`, mobile `ui.ts` claro/oscuro); `contacted` pasa de azul a **naranja** (distinto del ámbar de `pending`), `lost` de gris a **rojo** en web (`ClientsTable`/`ClientDrawer`) y reportes (`FUNNEL_COLORS`) — la app ya lo tenía rojo. `STATUS_TONE` unificado en `web/src/lib/types.ts` (antes duplicado). SEM-2: `ClientCard.tsx` mobile ya no marca "vencido" en `won`/`lost` (la web ya lo hacía vía `isFollowUpOverdue`). Verificado en navegador (claro/oscuro), `tsc` OK web+mobile, `build` web OK. Rama `sem-1-semaforo`, PR aparte |
| 2026-07-11 | **WEB-8 — Paginación de ClientsTable**: a partir del video de WEB-26 (ver fila de abajo), se identificó que la tabla dibujaba los 46 clientes sin paginar como causa más probable del glitch de scroll. `ClientsTable.tsx`: se agregó `visibleCount`/`PAGE_SIZE=20`, tarjetas móvil y tabla desktop ahora renderizan `visible` (slice paginado) en vez de `filtered` completo, con botón "Cargar más"; se resetea a la primera página al cambiar cualquier filtro. La selección múltiple sigue actuando sobre todo lo filtrado (se renombró el botón "Seleccionar visibles" → "Seleccionar todos los filtrados" para que no confunda). `tsc`/`build` OK. **Sin probar en el celular real** — el login requiere Google OAuth real, no se pudo automatizar en esta sesión |
| 2026-07-11 | **Housekeeping + WEB-26 (mitigación)**: verificado que `main` está al día con `origin/main` (GIT-1 cerrado, docs desactualizados). Investigado WEB-26 con un agente de exploración: causa más probable encontrada es `color-mix()` en runtime generado por Tailwind v4 al usar modificadores de opacidad (`bg-primary/10`) sobre tokens oklch, en el `Badge` que se renderiza por cada fila de `/clientes`. Aplicado fix de bajo riesgo (tokens `--badge-*-bg` con alfa oklch literal en `globals.css`, usados en `Badge.tsx` en vez del modificador `/N`) — no cambia el resultado visual, elimina el cálculo repetido. **Hipótesis, no confirmado** en dispositivo real. `tsc`/`build` OK. Las 4 plantillas duplicadas de n8n y los 8 workflows viejos a desactivar quedaron documentados en `docs/STATE.md` pero sin ejecutar: este worktree no tiene `crm-secrets.local.env`/`n8n-ids.local` (locales, no versionados) — requieren `apikeyn8n` y deben resolverse desde el checkout principal |
| 2026-07-11 | **Sprint 1 — Migraciones base** (backend de PERM-1, PROF-1, CONT-1, PERM-2, PERM-3, NOTE-1): migraciones `0015`→`0021` en Supabase Cloud vía MCP. Rol `viewer` (helper `private.is_read_all()` en políticas SELECT de profiles/clients/interactions + `set_user_role` lo admite); columnas de perfil (`phone`, `secondary_email`, `notification_prefs`); `phone_2`/`email_2` en clientes; tabla `client_changes` con trigger de auditoría en `UPDATE`; bucket privado `interaction-attachments` + tabla `interaction_attachments` con RLS en tabla y en `storage.objects`; canal `'note'` en interacciones con `outcome` nullable. `get_advisors` limpio (solo agregó 2 índices de FK sugeridos). Tipos TS de `web/` y `mobile/` actualizados; 4 pantallas ajustadas por `outcome` ahora nullable. `tsc`/`build` OK en ambos. Falta la UI de cada pieza — es Sprint 2 |
| 2026-07-11 | **Probado el Sprint 1 + UXR-1/UXR-2** en local (Supabase real vía transacciones revertidas + Chrome contra `npm run dev`). Encontrados y corregidos 2 bugs reales: (1) el constraint de `NOTE-1` solo bloqueaba en una dirección — canales no-`'note'` podían insertarse sin `outcome` (migración `0022_fix_note_outcome_check.sql`, constraint simétrico). (2) El fallback del logo (`UXR-2`) nunca se activaba en la práctica: el `<img>` de SSR puede fallar antes de que React hidrate y conecte `onError`, perdiendo el evento — se agregó un chequeo en `useEffect` al montar (`Logo.tsx`). Ambos verificados end-to-end tras el fix. `tsc`/`build` OK |
| 2026-07-10 | **Gestión de roles desde Equipo**: activada la RPC `set_user_role` (existía sin usar) con protección anti-autodegradación y anti-último-admin (migración `0014`, probada con transacción de prueba revertida). Panel: botón "Hacer admin" en Vendedores y "Bajar a vendedor" en Administradores, con confirmación inline |
| 2026-07-10 | **UXR-1 + UXR-2**: login muestra el error de OAuth (`login/page.tsx` lee `?error=auth`, envuelto en `Suspense`); fix de clases de color rotas en el logo de respaldo (`brand/Logo.tsx`). `tsc`/`lint`/`build` OK (lint tiene 9 errores preexistentes en otros archivos no tocados, regla `react-hooks/set-state-in-effect`, fuera de alcance) |
| 2026-07-10 | **Contactos GHL para vendedores**: página `/vendedor/contactos-ghl` (nav + `GhlBrowser` con `selfAssignId` — importa siempre a su propia lista, sin selector de vendedor), APIs search/tags abiertas a sellers, RPC `ghl_import_status` (migración `0013`, security definer) para que la detección de "ya importado" sea global entre vendedores |
| 2026-07-10 | **Clientes: fix filtros + vista móvil**: combobox mostraba solo la opción seleccionada al abrir (filtraba por el texto de la selección); móvil con tarjetas + botones WhatsApp/Llamar/Email, filtros colapsables, sin CSV/stats/selección masiva |
| 2026-07-09 | **Invitaciones con email real**: edge function `invite-user` (Supabase Auth invite, valida superadmin), sección "Invitaciones pendientes" en Equipo con Reenviar/Quitar (RPC `uninvite_member`, migración `0012`), advertencias para cuentas no-Gmail, login alternativo por **enlace de email** (`signInWithOtp` + página `/auth/confirm` para links con hash). Pendiente menor: SMTP propio (el de Supabase manda pocos emails/hora desde noreply@mail.app.supabase.io) |
| 2026-07-09 | **Fix integración n8n (post-revisión)**: los flujos usaban `$credentials` en expresiones (n8n no lo permite → `p_secret` vacío, retry/auto-import fallaban en cada corrida). Ahora el secreto viaja por Header Auth nativa y los RPC lo leen de `request.headers` (nueva `private.n8n_request_secret()`). Además: guard anti-loop en `push_to_crm` (el write-back re-disparaba el push infinitamente), inbound preserva tags `crm-lite:` y no pisa el nombre, `mark_crm_dirty` incluye tags. Migración `0011` aplicada y registrada. **Write-back probado e2e** (alta + edición → `crm_contact_id` OK, 1 push por cambio, datos de prueba limpiados) |
| 2026-07-09 | **Integración n8n N8N-0→13**: carpeta `crm-lite/`, 8 flujos GHL + alertas + plantillas, credenciales webhook/integración, push con writeback, retry/inbound/auto-import/pipelines, batch rate-limit, docs `INTEGRACION-N8N.md`, `n8n/README.md`, web Configuración GHL |
| 2026-07-09 | Web `/clientes`: **rediseño UX** — stats mini, tabla sin selects inline (fila clickeable + badges), columna seguimiento, filtros vendedor/tag (combobox), drawer con WhatsApp/email/llamar + link GHL, CSV en modal con preview/dedup/plantilla. Doc: `docs/WEB-CLIENTES.md`. `tsc` OK |
| 2026-07-09 | Web `/contactos-ghl`: combobox tags, búsqueda auto, indicador “ya importado”, selección por fila/nombre |
| 2026-07-08 | Web: **modo vendedor completo** (ruteo por rol: vendedor → `/vendedor`). Mis pendientes + banner de progreso, Contactados (hoy/semana), ficha con **contactar** (wa.me/mailto/tel) + **registrar interacción** + historial, y agregar cliente. `next build` OK (16 rutas) |
| 2026-07-08 | Web — paridad con la app + más control: **ficha de cliente** (drawer con historial de interacciones + editar todo + borrar), **agregar cliente manual**, **filtro seguimientos vencidos**, y **página Configuración** (meta diaria, modo WhatsApp, zona horaria, administradores). `next build` OK |
| 2026-07-08 | **Rediseño premium web con shadcn/ui** (Base UI + tokens oklch, paleta azul, modo claro/oscuro con clase `.dark`, gráficos recharts, sombras/gradientes). `next build` OK |
| 2026-07-08 | Fix bug importar contactos GHL: índice único parcial → total en `crm_contact_id` (ON CONFLICT) — migración `0009` |
| 2026-07-07 | **Rediseño UX/UI**: web (tokens + modo claro/oscuro + UI kit + toasts + responsive + logo) y app (tema claro/oscuro + iconos + pulido + logo). Logo desde archivo documentado en README. tsc + lint OK |
| 2026-07-07 | **Web admin v1** (`web/`, Next.js 16): Login+gate superadmin, Inicio, Equipo, Clientes (filtros/CSV/reasignar), Contactos GHL, Reportes+CSV. tsc + lint OK |
| 2026-07-07 | **Integración GHL bidireccional**: PUSH (DB webhook `0007` → n8n → upsert) y PULL (buscar por tag → importar) probados end-to-end |
| 2026-07-07 | `origin` + `tags` en `clients` (migración `0006`); badges en la app; endurecimiento `push_to_crm` (`0008`) |
| 2026-07-07 | **N8N-1** (0.1): renombrado `ghl_*` → `crm_*` (migración 0005 + trigger/índice + `types.ts`) |
| 2026-07-07 | Seguridad: sacada la `apikeyn8n` de `mobile/.env` (versionado) → `crm-secrets.local.env` (git-ignored) |
| 2026-07-07 | **SEC-1**: rotado el secreto de Google OAuth (login OK con el nuevo) |
| 2026-07-07 | **SEC-2**: rotada la contraseña root del VPS Hostinger (desde el panel) |
| 2026-07-07 | Lanzador `iniciar-app.bat` (arranca Metro offline + IP fija) — modo oficial de arrancar la app |
| 2026-07-06 | Migración a Supabase Cloud (`CRM.LITE`): proyecto creado, migraciones 0001→0004, Google OAuth, `.env` |
| 2026-07-06 | Baja a Expo SDK 54 (compatibilidad Expo Go) |
| 2026-07-06 | Equipo por invitación (migración 0003 + pantallas) |
| 2026-07-06 | Banner motivacional meta/racha (migración 0004) |
| 2026-07-06 | `ARCHITECTURE.md` actualizado (multi-CRM vía n8n + web) |
